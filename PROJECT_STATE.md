# K1x PMTC Assessment -- Project State

**Purpose:** Authoritative running state. Read at every session start. Updated whenever an item opens or closes.

**Last updated:** 2026-08-31 13:03 EDT (Tracking-docs audit: PROJECT_STATE.md/SESSION_LOG.md/CLAUDE_problems.md were current; CLAUDE.md was stale (fixed -- mailer row + 4 new Key Decisions Log entries for today's real work). Two scratch files cleaned up. Awaiting Ben's go-ahead to commit.)

> **READ `AFTER_YOU_PULL.md` FIRST.**
> The tool now has a custom domain (`https://k1x-pmtc.geniusdrive.com/`), added
> by Tristen. Nothing about the existing deploy workflow changed, but there is
> one **launch-blocking app bug** described there: the Flask session cookie has
> no `SameSite`/`Secure` set, so the tool's session is dropped inside the K1x
> iframe embed and the wizard loses state. Tracked as Open Item #12.
> `AFTER_YOU_PULL.md` has the exact fix. Confirmed still unfixed as of this
> merge (`app/app/__init__.py` is untouched on both sides). Delete this banner
> once it is done.

---

## Project Status

**Phase:** Phases 0-4 complete (scaffold, routing, templates, calc engine). Full Profile -> Assessment -> Results click-through verified with the Flask test client. Committed and pushed by Ben from his own machine -- `4a71a69`. Phase 7 (auth) also resolved this session -- Ben confirmed no login, Auth0 scaffold removed entirely (not committed yet). Next up: Phase 5 (PPTX, blocked on Open Item #2) or Phase 6/8/10/11/12 depending on what Ben wants to prioritize.
**Production URL:** `https://k1x-pmtc.geniusdrive.com/` -- live 2026-08-28. Fallback (permanent, unchanged): `https://ssida3ob72gzabi2ai5r6kqzle0ltmcp.lambda-url.us-east-1.on.aws/`
**Hosting:** AWS Lambda behind a Function URL, with CloudFront in front for the custom domain and certificate. Account `019163347448`, us-east-1, `tool0001` bootstrap qualifier. DNS for `geniusdrive.com` is at GoDaddy, records added by hand.
**Deploy method:** CDK (`infra/`), two independent stacks -- `PmtcApp` (the Flask app) and `PmtcDomain` (CloudFront + domain). Each is created only when its own `cdk.json` context is filled in. See `infra/README.md`.
**Calc engine tier:** **Tier 1 -- hand-port to Python.** Resolved 2026-08-26. See CLAUDE.md.

| Phase | Description | Status |
|-------|-------------|--------|
| 0 | Pre-project setup (structural workbook pass, PPT audit) | **Done.** Repo cloned, blueprint renamed to `pmtc`, workbook copied into `app/WORKBOOK.xlsx`, `.env` created, dependencies verified installable, Flask app factory confirmed loading, committed and pushed by Ben from his own machine (commit `c4996eb`). |
| 1 | Design lock and wireframe | **Done -- locked.** Wireframe built and decision-logged (`wireframe/DESIGN_DECISIONS.md`, 38 sections). Ben confirmed 2026-08-28: design is locked, no further changes. |
| 2 | Flask scaffold | **Done.** `routes.py` rewritten for the confirmed routing plan: `GET /`, `GET/POST /profile`, `GET /edit_profile` (alias), `GET/POST /assessment`, `GET /results`, `POST /api/lead`. Old ITSM-flow routes (`/step1_profile`, `/challenges`, `/assumptions`, `/calculators`, `/download`, `/send_report`, `/api/recalc_investment`) removed. |
| 3 | Production templates and input forms | **Done.** `profile.html`/`assessment.html`/`results.html` ported from the approved wireframe verbatim (CSS, vendored SegmentSlider JS, base64 icon SVGs all preserved byte-for-byte) with surgical edits only: `<form>` wrapper + hidden inputs + submit-via-POST instead of `location.href`, static asset URLs via `url_for`, session prefill (`PREFILL_GOALS`/`PREFILL_RATINGS`), Results page's hardcoded demo numbers replaced with real computed values from `calculator.py`. Old starter templates (`assumptions.html`, `base.html`, `calculators.html`, `modal_base.html`, `step1_profile.html`, `step2_challenges.html`, `summary.html`) deleted. |
| 4 | Calculations engine (Tier 1 -- Python hand-port) | **Done.** `calculator.py` hand-ports the workbook's goal-weighting, strength-rank, weighted-gap-rank, peer-comparison, and archetype-band logic exactly (verified by hand-computing one test case against the algorithm and cross-checking the `T`/`S`/`U` column tie-break behavior). `run_calculation()` returns everything the Results page needs. See CLAUDE.md Key Decisions Log for the archetype-clamp and advice-table-deferral calls made along the way. |
| 5 | Output Report generation | **Complete and wired to a real illustrative scenario.** Format confirmed HTML-to-PDF. The original 12-page dummy-scenario mockup (built 12:46-13:05 EDT) turned out not to hold up under real design review -- generic non-K1x-branded cover, no real logo, thin flat backgrounds, a font-substitution overlap on the testimonials slide -- so rather than patch it, the design restarted page by page, checked against K1x's own real decks and a comparable third-party report instead of invented from scratch. 11 of 11 pages locked and saved to the repo at `Application/output_report/` in current order: `01-cover.html`, `02-goals.html`, `03-how-scored.html`, `04-capability.html`, `05-where-you-stand.html`, `06-solutions.html`, `07/08/09-roadmap.html`, `10-success-story.html`, `11-trust.html` -- reordered 2026-08-29 per Ben (methodology moved right after goals, ahead of both score views; both trust/proof pages moved to the back to close out the deck). Page 6/`06-solutions.html` is the deck's first genuinely data-driven page -- see CLAUDE.md Key Decisions Log (2026-08-29 21:33 EDT) for the `CAPABILITY_FIXES`/`GAPS` architecture. Pages 7-9/`07-09-roadmap.html` extend the same template-plus-instance pattern to a full per-capability maturity roadmap -- see CLAUDE.md Key Decisions Log (2026-08-29 23:09 EDT) for the architecture, the auto-measured card-height technique, and the visual treatment. **Page 12 (hero stats) cancelled 2026-08-30 per Ben -- the deck is final at 11 pages.** **2026-08-30: full cross-page data-consistency reconciliation completed against a real `run_calculation()` scenario (test1), then verified page-by-page against the live app's actual Jinja2+JS-executed rendering (one minor bug found and fixed -- page 4's legend was missing peer industry/count detail the live app shows), then all 11 pages re-wired a second time to Skywalker Industries, LLC -- Ben's own recurring real-world test company -- which now stands as the deck's permanent illustrative scenario, replacing the earlier placeholder content. See SESSION_LOG.md 2026-08-30 entries for full per-page detail.** Remaining work is wiring the finished, now-internally-consistent pages to real per-user session data (see Open Item #2). **2026-08-30: all 11 pages now also exist as real, verified Jinja2 templates (`*.tmpl.html` siblings) -- the data-binding layer itself is built and tested against 4 real scenarios; what's left is wiring that into the live delivery path (`results.html`/`mailer/generate.py`), not the templating work itself.** |
| 6 | Email delivery | **Mailer live and fully wired end to end -- `PmtcApp` redeployed 2026-08-28 21:33 EDT (Open Item #14b closed).** Q3 resolved: AWS SES, sending from `reports@geniusdrive.com`. New `PmtcMail` CDK stack (`infra/lib/mail-stack.ts`) plus a container Lambda (`mailer/`) that fills a deck, converts it to PDF with LibreOffice, and sends it as a raw-MIME attachment. `POST /api/lead` invokes it asynchronously via `emailer.py` (rewritten from the dead ITSM Gmail/SMTP stub, which imported a module that does not exist in this project). The deck it fills today is a placeholder single slide carrying the real computed figures -- Ben is building the real template, and swapping it in is a `COPY master.pptx` line plus a fill function, not an architecture change. Container built and the whole generate-then-convert path verified locally on ARM64 (fonts resolve to Carlito rather than the Liberation Mono fallback, 28KB PDF, visually checked); `/api/lead` wiring verified through the Flask test client in all three states (unconfigured, configured, invoke throws). **`PmtcMail` deployed 2026-08-28 19:53 EDT and verified with a real send: PDF converted in 3.9s, SES accepted it, and the message was confirmed delivered to the inbox (not spam) with the attachment intact. `PmtcApp` redeployed 2026-08-28 21:33 EDT with the IAM grant and `REPORT_MAILER_FUNCTION` env var live -- the app can now actually invoke the mailer when a visitor requests a report.** See Open Item #14 and `mailer/README.md`. |
| 7 | Auth scaffold | **Done -- removed.** Ben confirmed 2026-08-26/27 the tool has no login. Auth0 scaffold (`app/auth.py`, `AUTH_REQUIRED`/`AUTH0_*` config in `app/__init__.py`, `authlib` dependency, `.env`/`.env.example` Auth0 block) deleted rather than left disabled. Verified app still boots and all routes resolve with the Flask test client. Not yet committed -- Ben to commit from his own machine. |
| 8 | Hosting and deployment | **Done -- live.** Deployed 2026-08-28 to the shared AWS account (`019163347448`), Lambda behind a Function URL, via `infra/`'s CDK app (`AppStack`). Live at `https://ssida3ob72gzabi2ai5r6kqzle0ltmcp.lambda-url.us-east-1.on.aws/` -- Ben confirmed end-to-end (Profile -> Assessment -> Results) on the real deployed instance, not just a local dev server. Two real bugs caught and fixed before this deploy: a Windows-incompatible `cp` shell call in the Lambda bundling code, and a blanket-copy bug that would have shipped `.env` (real secrets) into the deployed function -- see CLAUDE_problems.md P044. **Custom domain live 2026-08-28** at `https://k1x-pmtc.geniusdrive.com/`, added as a second, independent CDK stack (`PmtcDomain`, `infra/lib/domain-stack.ts`) putting CloudFront in front of that same Function URL -- which stays public and is the permanent fallback. Built and deployed by Tristen on branch `klutetr/new-domain`; certificate requested standalone (not CloudFormation-managed) and both GoDaddy CNAMEs added by hand. One real bug caught at deploy: CloudFront rejects cookie/compression settings on a zero-TTL cache policy, see CLAUDE_problems.md P050. Email delivery (Q3) still open; secrets are still plain Lambda environment variables, not Secrets Manager (flagged as a future hardening step). See SESSION_LOG.md's 2026-08-28 14:49 EDT entry for the full deploy story and `infra/README.md` for redeploy steps. |
| 9 | Full report push | **Unblocked on the sending side** -- Q3 is resolved and the delivery pipeline is live end to end, app-side wiring included (Phase 6, Open Item #14 closed). What remains is Phase 5's half, now actually in progress rather than paused: the Output Report design has to be finished page by page and wired to real session data, at which point `mailer/generate.py` renders it instead of its placeholder. See Open Item #17 (mailer's own format conversion) and Open Item #2 (the report design itself). |
| 10 | Data capture | **Done.** `data_capture.py` rewritten from scratch against the schema Ben laid out in the workbook (`NR!G2:AM3`) and his own Google Sheet (columns A:AG, data from row 4, column A untouched). Row written/updated at `POST /assessment` (`capture_result()` -- appends the first time, updates in place on revise-and-resubmit); lead fields backfilled at `POST /api/lead` (`update_lead_info()`), with a fallback retry if the initial capture never landed. Live-verified end-to-end on Ben's own machine 2026-08-28 (real Google Sheets credentials, all three paths -- append, lead backfill, revise-and-resubmit update-in-place -- nominal); one real bug caught and fixed in the process (Windows lacks a built-in tz database, see CLAUDE_problems.md P042). Committed and pushed by Ben from his own machine -- `fbe0958`. See Q2 below and CLAUDE.md Key Decisions Log for the full schema and architecture. Deliberately not carried forward from the old module: notification-email-on-append (out of scope per Ben; revisit against `emailer.py` once Q3 is confirmed). |
| 11 | Workbook lifecycle setup | Pending |
| 12 | QA and delivery | Pending |

---

## Open Items

| # | Item | Status |
|---|------|--------|
| 1 | ~~Ben needs to commit the Phase 0 scaffold changes from his own machine~~ | **RESOLVED 2026-08-27 01:35 EDT.** Ben committed and pushed from his own machine -- `c4996eb`, clean diff (16 files, 38 insertions, 35 deletions), no line-ending pollution. Confirms the CRLF issue seen in this session's shell was purely a comparison artifact, not real file corruption. |
| 2 | ~~No PPTX report template.~~ Renamed "Output Report" per Ben 2026-08-28 -- format is HTML-to-PDF (see CLAUDE.md Key Decisions Log). The original 12-page dummy-scenario mockup is superseded; the design restarted page by page instead, checked against K1x's own real decks and a comparable third-party report. 11 of 11 pages locked and saved to `Application/output_report/`, in current order: `01-cover.html`, `02-goals.html`, `03-how-scored.html`, `04-capability.html`, `05-where-you-stand.html`, `06-solutions.html`, `07/08/09-roadmap.html` (roadmap parts 1-3), `10-success-story.html`, `11-trust.html`. Reordered 2026-08-29/30 per Ben: methodology (How This Is Scored) now runs right after goals, ahead of both score views; the two trust/proof pages (Success Story, Scaled. Trusted. Proven.) moved to the back of the deck to close it out, so all three external-proof pages read as one closing sequence instead of being split apart. Same pass also fixed a regression Ben caught -- an eyebrow (`.part-tag`) that had grown back into the roadmap pages, plus several em dashes across the roadmap pages and Solutions -- see CLAUDE_problems.md P055 for full RCA; both were already-established deck-wide rules being re-broken in freshly-built content, not new rules. Per-page build history and design techniques (x-axis widening on How This Is Scored, the Solutions page's `CAPABILITY_FIXES`/`GAPS` data architecture, the Roadmap pages' `CURRENT_DESCRIPTIONS`/`NEXT_STEP_ADVICE`/`CAPABILITY_LEVELS` architecture and auto-measured card heights, the shared visual treatments) are in CLAUDE.md's Key Decisions Log and SESSION_LOG.md, not repeated here. **Page 12 (hero stats) cancelled 2026-08-30 per Ben -- there will be no page 12; the deck is final at 11 pages.** The hero-stat sourcing research done for it (`wireframe/DESIGN_DECISIONS.md` §39-44) stays on record but is not going anywhere in this deck. **2026-08-30: cross-page data-consistency reconciliation is complete -- all 11 pages now derive from one real `run_calculation()` scenario apiece, first test1 (verified page-by-page against the live app's real Jinja2+JS-executed rendering, one minor bug found and fixed there), then Skywalker Industries, LLC, which now stands permanently as the deck's illustrative scenario. See SESSION_LOG.md 2026-08-30 entries.** `output_report/DECK_TRACKER.html` reflects the current order and is updated whenever a page locks or moves. | OPEN -- design is finished, internally consistent, and verified against the live app for one real scenario; what remains is wiring the finished 11-page design to real per-user session data instead of the fixed Skywalker Industries illustrative scenario. Q3/email delivery no longer blocks this -- resolved 2026-08-28 (Phase 6, Open Item #14). **2026-08-30: all 11 pages converted to real Jinja2 templates (`*.tmpl.html` siblings next to each locked page) and verified against 4 real `run_calculation()` scenarios (test1/test2/test3/Skywalker, 44 render combinations) with both a programmatic Playwright check and a visual spot-check -- zero real problems found. Deliberately not wired into `results.html`/`routes.py`/`mailer/generate.py` yet, per the confirmed scope -- that wiring is what remains open here. See SESSION_LOG.md 2026-08-30 02:45 EDT entry.** **2026-08-30 02:58 EDT: a standalone, read-only QA tool (`tools/preview_report.py`) now proves this against real visitor data -- it pulls a real Sheet row's raw inputs and re-runs them through the real calc engine and the same Jinja2 templates, with zero touch on `routes.py`/`results.html`/`mailer/generate.py`. Self-tested offline against known-good Skywalker data (exact match, zero mismatches). Live-Sheet verification is blocked from the bridge shell (network egress restriction on oauth2.googleapis.com, not a bug) and needs Ben to run it from his own terminal -- see SESSION_LOG.md 2026-08-30 02:58 EDT entry.** **2026-08-30 03:14 EDT: verified live on Ben's own machine against real row 11 (Comp 1 / Accounting) -- confirmed working end to end, including one real bug found and fixed along the way (P058, a Windows-only UTF-8-encoding gap that corrupted the (R) symbol on page 6). This tool is now a confirmed-working way to preview any real visitor's deck; the live-wiring half of Open Item #2 (`routes.py`/`results.html`/`mailer/generate.py`) remains the one open item.** **2026-08-30 14:12 EDT: the `results`/`goals` contract gap is now closed on the app side -- `emailer.send_report()` accepts and forwards `session['goals']` alongside `results`/`lead`, so once the mailer renders the real templates it will have the visitor's actual goal priorities available, not just computed results. The templates themselves still need the live-wiring described above; this only unblocks their page-2 data dependency ahead of time.** |
| 3 | ~~`check_files.sh` / `check_structure.py` (the mandated post-write verification scripts) hardcode file paths from a prior project (`itsmbvf`/ITSMweb), not this project.~~ | **RESOLVED 2026-08-26/27.** All five scripts (`check_files.sh`, `check_structure.py`, `check_css.py`, `check_js.py`, `check_routes.py`) rewritten for this project's real structure: 3 templates (`app/templates/pmtc/{profile,assessment,results}.html`, each self-contained, no shared base.html) and 5 modules (`app/blueprints/pmtc/{routes,calculator,data_capture,emailer,report}.py`). `check_css.py`/`check_js.py` redesigned per-file (no shared base.html to check coverage against). `check_routes.py` rewritten around the real session model (`profile`/`goals`/`ratings`/`results`) and `run_calculation()` signature. Ran clean end-to-end (`bash check_files.sh` exits 0); fresh `.check_baseline.json` generated. Not yet committed -- Ben to commit from his own machine, `.check_baseline.json` included. |
| 4 | ~~Routing plan for Phase 2 needs Ben's confirmation~~ | **RESOLVED 2026-08-27.** Ben confirmed ("routing plan looks good"); built as Phases 2/3/4 this session. |
| 6 | ~~This session did not commit its Phase 2/3/4 work~~ | **RESOLVED 2026-08-27.** Ben committed and pushed from his own machine -- `4a71a69` (13 files changed, 3179 insertions, 627 deletions). Hit a stale `.git/index.lock` from this session's earlier git commands first; removed it, then the commit/push went through clean. |
| 7 | Scoping decisions Q1 (hosting) and Q3 (email delivery) were unconfirmed defaults. | **RESOLVED 2026-08-28.** Q1 resolved earlier the same day (AWS Lambda). Q3 resolved this session (AWS SES) and built: `POST /api/lead` now both writes the lead to Google Sheets and asks the mailer for a report, so the report-email half is no longer a no-op. All six scoping decisions Q1-Q6 are now settled except Q6, which stays moot until Phase 5 unblocks. |
| 5 | This session's bridged shell's local git is unreliable beyond read-only commands -- after Ben's commit/push, a `git log` here triggered "unable to unlink .git/index.lock: Operation not permitted," and `git status` afterward showed every file as modified again (stale/permission-locked index, not a real state problem -- the commit itself is confirmed good both by Ben's terminal output and by `git log` showing `c4996eb` present). This is exactly the failure mode STANDING_RULES.md's git section warns about. Standing practice going forward: read-only git commands only from this session (`git log`, `git show`); all `add`/`commit`/`push` from Ben's machine. | Noted, not blocking -- informational |
| 8 | The `itsmbvf@gmail.com` personal-Gmail workaround for the PMTC Google Cloud service account (used to bypass the geniusdrive.com org's `iam.disableServiceAccountKeyCreation` policy -- see Q2/CLAUDE.md Key Decisions Log) is not a scalable pattern across future client projects: it scatters unrelated clients' credentials under an ad-hoc personal account named for a different client, has no organizational governance (no central audit trail, no clean offboarding), and is a single point of failure -- if that one personal account is ever locked or lost, every project riding on it breaks at once. Two real fixes discussed, neither actioned: (1) get an `iam.disableServiceAccountKeyCreation` exception on the actual geniusdrive.com org, ideally at a GCP resource-hierarchy folder level (a dedicated "client projects" folder) so future projects inherit it automatically rather than repeating this workaround; (2) skip downloadable service-account keys entirely via GCP Workload Identity Federation with AWS as the external identity provider -- since PMTC (and future client tools) will run on AWS with its own IAM role, that role could impersonate the GCP service account with no long-lived key stored anywhere, which also sidesteps the org policy rather than routing around it. Not blocking PMTC today -- current setup (`itsmbvf@gmail.com`-owned service account) is built, tested, and working; this is a forward-looking process/governance question for Ben to decide on his own timeline, not a PMTC-specific defect. | OPEN -- forward-looking, not blocking |
| 9 | ~~`infra/` (the CDK deploy code) was untracked in any git repo -- only `app/` was.~~ | **RESOLVED 2026-08-28 16:09 EDT.** Folded into the same repo as `app/` by relocating `.git` up one level (Ben authorized: "Yes, proceed with your clean fix"). Caught and fixed a secret-leak risk first: `infra/` had no `.gitignore`, and `infra/cdk.json` holds real deploy secrets -- see CLAUDE_problems.md P046. Ben committed and pushed from his own machine -- `fbe0958..bf74e61`. `git status` confirmed clean before commit: all real `app/` files renamed (not delete+recreate, history preserved), `cdk.json`/`node_modules/`/`cdk.out/` correctly absent from what was staged. One cosmetic-only exception: `WORKBOOK.xlsx` showed as delete+add rather than rename (Git's similarity heuristic on the LFS-tracked binary) -- history is still intact via `git log --follow`, no action needed. |
| 10 | ~~Uncommitted bug/design fixes pending Ben's next commit + redeploy: (1) breadcrumb navigation now links forward as well as back; (2) Peer Leaders score fix + Excel-rounding fix (`calculator.py`, see P047); (3) `app/WORKBOOK.xlsx` refreshed; (4) Results page narrative redesigned into a card (`.narrative-card`); (5) Strengths/Gaps cards' fine print relocated under the header; (6) maturity curve legend centered + x-axis labels reformatted.~~ | **RESOLVED 2026-08-28 17:47 EDT -- fully live.** Committed/pushed `bf74e61..b917423` at 17:37 EDT, then Ben ran `npx cdk synth` (clean) and `npx cdk deploy PmtcApp` (17.27s deploy, 47.92s total) from his own machine. Same `AppUrl` as before (`https://ssida3ob72gzabi2ai5r6kqzle0ltmcp.lambda-url.us-east-1.on.aws/`) -- Function URL is stable across a code-only redeploy, no DNS/URL change. Ben's manual click-through (per the stack's own `NextStep` output) found one real regression within minutes -- see new Open Item #11 (P049). |
| 11 | Live-site regression found by Ben's post-deploy click-through: the "Where Your {industry} Peer Leaders Ranked" eyebrow (`.ysc-peer-eyebrow-row`) overlapped the new narrative card's border for the two longest `INDUSTRIES` values ("Family Office / Wealth Management", "Tax Exempt / Non-Profit") -- a `white-space:nowrap` left over from before the narrative card had a visible background, now overflowing onto something visible. See CLAUDE_problems.md P049 for full root cause. | **RESOLVED 2026-08-28 18:07 EDT -- fully live.** Committed/pushed `b917423..5d56eb4` at 18:01 EDT, deployed via `cdk deploy PmtcApp` and QA'd by Ben directly on the live site. |
| 12 | ~~The Flask session cookie has no `SameSite`/`Secure` set, so the iframe embed will not work.~~ **RESOLVED 2026-08-28 19:57 EDT -- fully verified, launch blocker cleared.** Fixed in `app/app/__init__.py`, committed/pushed `2d2adf3`, deployed via `cdk deploy PmtcApp`. Ben ran the real cross-site iframe test (`iframe_embed_test.html`, a local `file://` page embedding `https://k1x-pmtc.geniusdrive.com/`) and confirmed Profile -> Assessment -> Results all work inside the frame -- the actual embed scenario, not just a header check. |
| 13 | Pages are served uncompressed (~65KB for `/profile`) because CloudFront refuses `EnableAcceptEncodingGzip` on a caching-disabled policy (CLAUDE_problems.md P050) and every page here must be uncached. Deliberately not solved by loosening the cache policy: a nonzero TTL on a route that emits `Set-Cookie` (`GET /` calls `session.clear()`) risks serving one visitor's session cookie to the next. **2026-08-30: attempted via `flask_compress.Compress(app)` + `Flask-Compress==1.24`, deployed, and reverted the same afternoon -- broke the live site.** Local verification (Flask test client, `check_routes.py` against a real venv) passed, and a pre-deploy check confirmed its `backports.zstd` dependency had a `manylinux2014_aarch64` wheel -- but the real deployed Lambda failed at import with `No module named 'backports'` (~21 min outage, 13:31-13:52 EDT). Reverted cleanly (`app/app/__init__.py` and both requirements files back to original); redeployed and confirmed live by Ben. Full RCA in CLAUDE_problems.md P059, including why the pre-deploy check (which used the wrong Python version, 3.10 vs Lambda's real 3.13) wasn't sufficient. **If revisited: pin `Flask-Compress==1.14` (last version with no zstd dependency at all -- brotli/gzip alone already delivered the entire measured win) and verify the actual built Lambda artifact, not just a local reproduction, before redeploying.** | OPEN -- reverted after a live outage; see P059 before retrying |
| 14 | **`PmtcMail` is deployed and verified end to end. The remaining half is a `PmtcApp` redeploy, which only Ben can run.** (a) **DONE 2026-08-28 19:53 EDT.** `bash infra/deploy-mail.sh` succeeded on the second attempt (57.89s), after the first rolled back on an account guardrail -- see CLAUDE_problems.md P052 and Open Item #16. Verified with a real send rather than a synth: `python3 mailer/try_mailer.py tklute@geniusdrive.com` returned `{"ok": true}`; the function's log shows the PDF converted in 3.9s (28,852 bytes, 368MB peak of 3008MB, 2.5s cold start) and SES accepted it with message id `010001a04ad06b1d-d8ddbc5c-...`; and the message was confirmed **actually delivered to the inbox, not spam**, carrying `K1x Private Market Tax Capability Assessment.pdf` with the correct body and figures. (b) **DONE 2026-08-28 21:33 EDT.** Ben ran `npx cdk deploy PmtcApp` from his own machine (with `aws configure export-credentials --format powershell | Invoke-Expression` first, per Tristen, to get CDK working credentials in the PowerShell session) -- deploy succeeded. This carries the app-side half: the `lambda:InvokeFunction` grant and the `REPORT_MAILER_FUNCTION` environment variable. Note P049 (#11) and the SameSite cookie fix (#12) were already live from earlier tonight's separate redeploys, and #13 (`flask-compress`) is still only documented, not implemented -- neither rode along with this one. **Still outstanding, unrelated to this item: confirm the SNS subscription email AWS sent to `tklute@geniusdrive.com` on the `PmtcMail` deploy**, or bounces and complaints go unwatched. | (a) DONE, (b) DONE |
| 16 | Small pending mail-stack change, uncommitted and not yet deployed: the mailer's CloudWatch log group had no explicit name, so CDK generated `PmtcMail-ReportMailerLogs6692A03F-tNeZuXsBFWMG`. Found immediately after the first successful send, when the log could not be tailed at the obvious path. That matters more here than it looks: this function is designed to fail silently, so its log is the only place a report that never arrived shows up, and an unguessable log group name is a real obstacle at exactly the moment somebody is debugging one. Fixed in `mail-stack.ts` with `logGroupName: '/aws/lambda/pmtc-report-mailer'`, the path Lambda would have used itself; `try_mailer.py` now also reads the live log group off the function rather than assuming a path. **Needs one more `bash infra/deploy-mail.sh` to take effect**, which does not warrant its own trip -- let it ride along with the deploy that sets the blind-copy address once K1x names someone (Open Item #15). Note this replaces the log group, so the existing few log entries are lost; there is nothing in them worth keeping. | OPEN -- low priority, batch with #15 |
| 17 | **The mailer renders the wrong format: it was built for PPTX-to-PDF (python-pptx + LibreOffice) when the agreed Output Report format is HTML-to-PDF (Playwright/headless Chromium).** Not a mistake in the delivery design, which is unaffected -- SES, the sending identity decision, the container Lambda, the IAM invoke, the async-with-retries-off contract, `emailer.py`, the `/api/lead` wiring, the payload contract and `handler.py`'s compose/send/log path are all format-agnostic and stay exactly as they are. What is wrong is what sits inside the container: `mailer/Dockerfile` installs LibreOffice and python-pptx, and `mailer/generate.py` draws slides. Cause: the mail work was built 2026-08-28 18:5x-20:00 EDT against `a05c99f`, and the doc correction establishing HTML-to-PDF (P051, commit `1d83e93`) landed on the other line at 18:20 EDT, so this session never saw it -- the two lines only met at this merge. A container Lambda remains right either way, since Chromium does not fit a zip any more than LibreOffice does, and `generate(data, out_path)` was deliberately given a format-agnostic signature so `handler.py` does not change. Converting it also makes the placeholder deck redundant: the real 12-page mockup is already HTML, which is exactly what a Chromium renderer wants as input. | **Code converted 2026-08-31 -- not yet deployed.** `mailer/generate.py`/`handler.py`/`Dockerfile`/`infra/lib/mail-stack.ts` rewritten to render the real 11-page `output_report/*.tmpl.html` deck via headless Chromium and write PDF directly, no LibreOffice step. Render logic verified end to end against the real Skywalker scenario (real 11-page/5.9MB PDF produced, page count checked, pages 1/3/6 visually inspected -- correct branding, correct data, P061 footer fix confirmed carried through). The actual container build (Debian/ARM64/Playwright/awslambdaric) has NOT been verified -- no Docker daemon available outside Ben's own machine. `cdk deploy PmtcMail` from Ben's machine is the first real build; a real test send via `try_mailer.py` right after that deploy,  **2026-08-31 12:36 EDT: LIVE and VERIFIED.** `cdk deploy PmtcMail` succeeded on Ben's machine (real ARM64 target, Docker build fully validated including `playwright install --with-deps chromium`). First real Lambda invocation crashed (`TargetClosedError: Browser.new_page: Target page, context or browser has been closed` -- Chromium's zygote-fork process model doesn't survive Lambda's sandbox); fixed by adding `--disable-gpu`/`--single-process`/`--no-zygote` to the `chromium.launch()` args in `generate.py`. Redeployed, retested through the real live app end to end -- **real email arrived with the correct 11-page deck, including actual session data.** **This item is RESOLVED.** Fix is applied locally on Ben's machine, not yet committed/pushed -- see SESSION_LOG.md 2026-08-31 12:36 EDT entry. |
| 15 | Two mail decisions are open, both go-live steps rather than oversights. (a) **The sender is a Genius Drive address.** A K1x prospect filling in a K1x-branded tool embedded on a K1x page receives mail from `reports@geniusdrive.com`. Sending as K1x means verifying a domain in K1x's own DNS, which is a dependency on their team and an unknown wait; the sibling SMOMA project weighed the identical trade and took the domain we control. (b) **Nobody is blind-copied yet** -- Tristen is waiting to hear from K1x who should be. The value lives in `infra/cdk.json` and the committed `infra/cdk.example.json` rather than as a deploy-script argument, deliberately: the copy is invisible in every message it appears in, so a deploy that dropped an argument would look completely normal until the reader asked why reports had stopped. Setting it is one `bash infra/deploy-mail.sh` with no code change. Related: when K1x names the person the report should come from, add them to `SIGNATURE` in `mailer/handler.py` **and** re-point `replyTo` in the same change -- today `Reply-To` is the only route out of the message, which is a property worth keeping until go-live so that test replies do not land on the client. | OPEN -- needs K1x |
| 18 | Mobile UI bug on the capability-assessment sliders: below-the-bar tick labels (one per segment on a CSS grid, no width constraint per column) run on into each other on narrow viewports since long level names have nowhere to wrap. Ben asked for a mobile-only fix. Client's actual requested design (via Ben): numerals 0-5 under every segment, plus a floating label above the currently-selected segment; iterated visual treatment per Ben's direction to a flat rectangle (no tail), `var(--paper)` background, `var(--signal)` bottom band, `var(--signal-ink)` text -- the same accent pairing already used by the breadcrumb's current-step pill. Ben confirmed the design ("this is acceptable"). Built into `app/app/templates/pmtc/assessment.html`'s own inlined SegmentSlider copy (CSS + `_build()`/`paint()` JS, `@media (max-width:760px)` matching this project's established breakpoint); `profile.html`'s separate copy untouched, no change to desktop. Verified two ways: `node --check` on the extracted script block, and a full Flask-test-client-plus-Playwright pass against the REAL rendered `/assessment` page (real session data, real Jinja2 render) at both 390px and 1200px, clicking through segment 0, a middle segment, and segment 5 -- zero JS console/page errors, badge and numerals track correctly with no overlap or edge overhang, desktop confirmed unchanged. See SESSION_LOG.md 2026-08-30 15:07 EDT entry for full detail. | **RESOLVED 2026-08-30 19:00 EDT -- fully live.** Deployed via `cdk deploy PmtcApp` (17.17s, same AppUrl); Ben confirmed the deploy output clean |
| 19 | Two more Results-page maturity-curve fixes Ben asked for, both in `calculator.py`. (a) The "Recommended" mark was a fixed `RECOMMENDED_TARGET = 3.6` for every visitor. Replaced with `recommended_target(your_score) = min(5, max(3, ceil(your_score) + 1))` per Ben's formula and worked examples (2.6->4, 1.3->3, 3.2->5); the cap at 5 (the scale's own top) was a gap in Ben's examples, raised and confirmed with him before adding it. (b) The "Peer Leaders" mark/figures (`PEER_SCORES`) were a stale flat per-capability constant ported once from the original workbook, not live-wired to anything. Ben supplied updated per-capability figures (a screenshot of a refreshed "PEER LEADERS" column); applied directly, checked the new mean (4.23, rounds to 4.2) against the total row on his screenshot before applying. Both fixes verified: `run_calculation()` called directly against Ben's worked examples plus two uncovered edge cases (4.6 -> caps at 5, 0.0 -> floors at 3), all correct; project's own `check_files.sh` (all 5 layers) clean; the real `/results` route rendered through Flask's test client with a full seeded session, actual output HTML confirmed to contain `chart.render({ now: 2.6, target: 4, peer: 4.2 })` verbatim. See SESSION_LOG.md 2026-08-30 15:28 EDT entry. | **RESOLVED 2026-08-30 19:00 EDT -- fully live.** Deployed via `cdk deploy PmtcApp` (17.17s, same AppUrl); Ben confirmed the deploy output clean |
| 20 | **P060 -- data-capture column skew, recurrence of P045 in the opposite direction, with real data loss.** Ben caught 4 live Google Sheet rows (12-15) that landed one column right of their intended B:AC range -- the same `append_row()`/`table_range` auto-detection P045 already fixed once (2026-08-28, that time drifting one column LEFT), now confirmed to drift in the opposite direction under the identical unchanged code. Row 15 lost real data: its true "Results GAP capability 3" value was overwritten by `update_lead_info()`'s fixed-position lead-info write once the row had already drifted into that column. Recovered the lost value (re-ran the row's own goals/ratings through `run_calculation()`, confirmed 6 other stored fields matched exactly first). Root-caused, fully written up as CLAUDE_problems.md P060. Fixed the root cause in `data_capture.py`: the append path no longer uses any auto-detecting append -- it computes the real next row via `sheet.col_values(2)` and writes with an explicit-range `sheet.update()`, the same mechanism the revise-and-resubmit path already used with zero drift across every row this project. Verified with a mock-sheet unit test and the full `check_files.sh`. Built `tools/repair_row_skew.py` (preview-by-default, `--apply`-gated) to fix rows 12-15 in the live Sheet; Ben confirmed to proceed with the repair and to leave rows 12/13 (an exact duplicate submission) as separate rows. Ben ran the repair with `--apply` and confirmed success; independently re-verified via a follow-up `read_file_content` call on the live Sheet -- rows 12-15 now show correctly column-aligned data, with row 15's recovered gap3 value ("Data Extraction") landing correctly ahead of the still-intact lead fields (Wiley/Coyote/bpinsky@geniusdrive.com/No). See SESSION_LOG.md 2026-08-30 19:51 EDT and 19:57 EDT entries. | **RESOLVED 2026-08-30 20:07 EDT -- fully live.** Code fix committed (`9ca57b9`), pushed (`176f26b..9ca57b9`), and deployed via `cdk deploy PmtcApp` (17.12s deploy, 92.6s total, same AppUrl `https://ssida3ob72gzabi2ai5r6kqzle0ltmcp.lambda-url.us-east-1.on.aws/`). Sheet repair independently re-verified -- rows 12-15 correctly aligned, recovered row-15 gap3 value confirmed in place. One remaining verification step noted in CLAUDE_problems.md P060: confirm the fix against a real live capture through the deployed app. |

| 21 | **P061 -- slide 3 (`output_report/03-how-scored.html`/`.tmpl.html`) footer looked off-brand, two independent causes.** Ben caught it first as a color/border mismatch (footer inheriting the deck's light-page style instead of the dark-page style page 5 already uses on its own matching dark photo background) -- fixed with an explicit override matching page 5 exactly. Ben then caught a second issue in the Comp_1 preview specifically: the footer font itself was rendering serif instead of the deck's Outfit sans. Root-caused via a controlled A/B Playwright render plus a Python `html.parser` DOM-ancestry check across all 11 `.tmpl.html` pages: only page 3's Jinja2 template had a stray duplicate `</div>` that pushed `.page-footer` outside `.page` in the DOM, breaking the `font-family` inheritance every other page relies on (the static, hand-authored `.html` sibling was already correctly structured -- only the Jinja2 template had the bug). **Correction:** checked afterward and this template isn't actually wired into the live mailer yet (Open Item #17, still open) -- today it's used only by the offline `preview_report.py` QA tool, so this didn't reach any real deliverable. Still the right fix (will matter once #17 lands), just not the urgency first claimed. Also found and fixed, unrelated: `tools/preview_output/11_Comp_1/assets` was a broken symlink (corrupted target) instead of the real copy `preview_report.py` itself already does deliberately to avoid exactly this Windows fragility -- replaced with a real copy. Rebuilt and re-delivered `Comp_1_PMTC_Capability_Report_PREVIEW.pdf`; also noted, not blocking, that `output_report/assets/fonts/` is missing `Outfit-Medium.ttf` (currently harmless -- browsers fall back to a sibling Outfit weight rather than a different font family). Full writeup: CLAUDE_problems.md P061. | **RESOLVED -- both fixes verified, committed (`160c0f0`) and pushed by Ben from his own machine (`9ca57b9..160c0f0 main -> main`, confirmed).** Not deployed anywhere -- this template is not yet wired into the live mailer (Open Item #17, still open); will matter once #17 lands. |

---

## Scoping Decisions

| # | Question | Status | Decision |
|---|----------|--------|----------|
| Q1 | Hosting platform | **RESOLVED 2026-08-28 -- AWS Lambda** | Ben confirmed AWS Lambda as the specific deploy path (narrowing the earlier "AWS, service TBD" call). Flask cannot run on Lambda unmodified -- needs an ASGI/WSGI-to-Lambda adapter (e.g. `awslambdaric` + a WSGI shim, or Zappa/Mangum-style wrapper) and a Lambda Function URL or API Gateway in front of it; the AWS handoff kit's `StaticSiteStack` (S3+CloudFront) does not apply to this app and CloudFront would be kept, if at all, only for a custom-domain cert. Not yet built -- Phase 8 still Pending. See CLAUDE.md Key Decisions Log. |
| Q2 | Data capture | **RESOLVED 2026-08-28** | Google Sheets, confirmed and built. Ben set up the target Sheet himself and defined the exact schema in the workbook (`NR!G2:AM3`, mirrored 1:1 onto Sheet columns A:AG with a fixed 6-column offset, data starting row 4). `data_capture.py` rewritten to match exactly -- see CLAUDE.md Key Decisions Log for the full column mapping and the append/update architecture. |
| Q3 | Email delivery | **RESOLVED 2026-08-28 -- AWS SES** | Tristen confirmed the report is emailed as a PDF, same pattern as the sibling SMOMA project. SES rather than the template default of Gmail SMTP (`modules/email_gmail_smtp.md`), for two reasons that are stronger here than they were there: from Lambda, SES is an IAM permission with no password to store, inject, leak or rotate, whereas the Gmail path needs an app password living somewhere (against the standing rule about plaintext secrets); and the queue-bound half of the setup is already paid for -- `geniusdrive.com` is a verified, DKIM-signed SES identity in this same account (`019163347448`/us-east-1) and the account already has production access, both done by the sibling project. Sender `reports@geniusdrive.com`, Reply-To and bounce notices to `tklute@geniusdrive.com`, blind copy empty pending K1x naming someone. See CLAUDE.md Key Decisions Log and `mailer/README.md`. |
| Q4 | Authentication | **RESOLVED** | Ben confirmed: no login for this tool. Auth0 scaffold removed entirely (2026-08-26/27) rather than left disabled -- see Phase 7 row and CLAUDE.md Key Decisions Log. |
| Q5 | Calc engine tier (1/2/3) | **RESOLVED** | Tier 1 -- hand-port to Python. See CLAUDE.md Key Decisions Log. |
| Q6 | ~~Chart approach for PPT (flattened image vs. native shapes)~~ | **RESOLVED -- moot, corrected 2026-08-28.** Format is HTML-to-PDF, not native PPTX -- there's no flatten-vs-native-shapes choice to make. The mockup already ports `results.html`'s real SVG curve chart and bar chart directly (in-browser render, captured to PDF via Playwright/headless Chromium). | N/A -- already built this way |

---

## Key Decisions Log

See `CLAUDE.md` Key Decisions Log for the full write-up of each. Summary:

- Tier 1 calc engine (2026-08-26)
- 10 active capability dimensions, confirmed via workbook-wide red-fill audit (2026-08-26/27)
- 6 active goals, same red-fill convention (2026-08-26/27)
- None = literal 0, no N/A concept -- resolves `OPEN_QUESTIONS.md` entirely (2026-08-27)
- Peer comparison data is seed/placeholder, implement as-is (2026-08-27)
- Total-score and peer-score averages re-derived in Python, not copied verbatim from the workbook's own (stale, 12-row) formulas (2026-08-27)
- Output Report format is HTML-to-PDF, not PPTX -- a 12-page mockup already built and delivered, paused pending live-data wiring + Q3 (2026-08-26, refined 2026-08-28 -- supersedes the original "PPTX report phase deferred" framing, see CLAUDE.md Key Decisions Log)
- Phase 2 will not reuse the starter's ITSM-shaped routes/templates as-is -- this project's real flow (3 pages, no investment model) needs its own routing plan (2026-08-27, pending Ben's confirmation)
- No authentication -- Auth0 scaffold removed from the codebase entirely, not just left disabled (2026-08-26/27)
- Email delivery via AWS SES on a separate container Lambda, invoked asynchronously by the app, reusing the already-verified `geniusdrive.com` identity rather than declaring a second one (2026-08-28)
- PMTC migrated to its own dedicated repo (`k1x-pmtc-assessment`) -- discovered while fixing Open Item #3 that step 3 of README_first.md ("create a new GitHub repo for this project") had been skipped, and every PMTC commit had been landing directly on the shared `web-project-starter` repo's main branch. Full history preserved (pushed to the new repo, plus a safety tag on the old one before reset). `web-project-starter` reset back to its last generic commit. (2026-08-26/27)

---

## Authoritative Source Registry

| Timestamp (ET) | Commit / Reload | Description |
|----------------|------------------|-------------|
| 2026-08-31 12:59 EDT | `1888a1c`..`0a454d2` | **K1x BCC recipients committed and pushed by Ben from his own machine (`1888a1c..0a454d2 main -> main`, 6 objects, 2.78 KiB, confirmed success).** `infra/cdk.example.json`'s `bcc` context key set to `tiana.maher@k1x.io,scott.turner@k1x.io` (was empty pending K1x naming someone). No code change -- the `REPORT_BCC`/`handler.py` plumbing already existed. **Deployed and verified live before this push** -- `cdk deploy PmtcMail` (env-var-only, 16.74s), and a real send confirmed via CloudWatch: "copied to tiana.maher@k1x.io, scott.turner@k1x.io".
| 2026-08-31 12:39 EDT | `62c3167`..`1888a1c` | **Real mailer fix committed from the bridge shell with Ben's go-ahead, pushed by Ben from his own machine (`62c3167..1888a1c main -> main`, 8 objects, 11.13 KiB, confirmed success).** Fixes the Chromium-on-Lambda crash (TargetClosedError at browser.new_page -- added --disable-gpu/--single-process/--no-zygote to mailer/generate.py's chromium.launch()), adds the repo-root .dockerignore that fixes the cdk deploy build-context stall, and adds the missing goals key to try_mailer.py's sample payload. **Deployed and verified live before this push** -- cdk deploy PmtcMail succeeded, and a real live-app test delivered the correct 11-page deck with real session data, twice. Open Item #2/#17 is now fully resolved.
| 2026-08-31 10:45 EDT | `30e1ab0`..`62c3167` | **Open Item #2/#17 -- real HTML-to-PDF Output Report mailer, pushed live.** Ben ran `git push origin main` from his own terminal: `30e1ab0..62c3167`, 23 objects (12 new), 14.25 KiB, clean. `mailer/generate.py`/`handler.py`/`Dockerfile` rewritten to render the real 11-page `output_report/*.tmpl.html` deck via headless Chromium and write PDF directly, replacing the python-pptx/LibreOffice placeholder; `infra/lib/mail-stack.ts`'s Docker build context widened to the repo root so the image can see `output_report/`. Render logic verified end to end against the real Skywalker scenario before this push (real 11-page/5.9MB PDF, page count checked, pages 1/3/6 visually inspected). **Not yet deployed** -- `cdk deploy PmtcMail` (the first real build of the new container) and a real `try_mailer.py` test send are the next, still-pending steps, ahead of a client meeting today. |
| 2026-08-31 09:57 EDT | `160c0f0`..`30e1ab0` | **Documentation-only commit, pushed live.** Ben ran `git push origin main` from his own terminal: `160c0f0..30e1ab0`, 11 objects (6 new), 7.14 KiB, clean. Fixes PROJECT_STATE.md's stale Authoritative Source Registry/current-commit line (was still showing `176f26b`; added the missing `9ca57b9`/`160c0f0` rows) and logs CLAUDE_problems.md P062 (AskUserQuestion picker misuse + proceeding on an unconfirmed plan). No app/infra code changed, nothing to redeploy. |
| 2026-08-30 22:52 EDT | `9ca57b9`..`160c0f0` | **P061 fix (slide 3 footer off-brand, two causes) committed from the bridge shell with Ben's go-ahead, pushed to GitHub live.** Adds the dark-page `.page-footer` color/border override to `output_report/03-how-scored.html`/`.tmpl.html` (matching page 5's existing treatment) and removes the stray duplicate `</div>` in the `.tmpl.html` that had pushed `.page-footer` outside `.page` in the DOM, breaking font inheritance to a serif fallback. Push confirmed via Ben's own terminal (`9ca57b9..160c0f0 main -> main`, success). Not deployed anywhere -- this template is not yet wired into the live mailer (Open Item #17, still open); today it's used only by the offline `tools/preview_report.py` QA tool. Full RCA: CLAUDE_problems.md P061. |
| 2026-08-30 20:00 EDT | `176f26b`..`9ca57b9` | **P060 fix (data-capture column skew, real data loss recovered) committed and pushed by Ben from his own machine, then deployed.** `data_capture.py`'s append path no longer uses `append_row()`/`table_range` auto-detection -- computes the real next row via `sheet.col_values(2)` and writes with an explicit-range `sheet.update()`, the same mechanism the revise-and-resubmit path already used with zero drift. Pushed `176f26b..9ca57b9`, then deployed via `cdk deploy PmtcApp` (17.12s deploy, 92.6s total, same `AppUrl`) -- confirmed live by Ben. Sheet repair (rows 12-15) independently re-verified. Full RCA: CLAUDE_problems.md P060. |
| 2026-08-30 19:00 EDT | `cdk deploy PmtcApp` | Redeployed Lambda with `176f26b`'s code -- mobile capability-slider redesign (Open Item #18) and maturity-curve Recommended-formula/Peer-Leaders-refresh fixes (Open Item #19) now live. Clean deploy, 17.17s deployment / 43.96s total, same `AppUrl` and stack -- no DNS/URL change on a code-only redeploy. Ben's terminal was in `app/` (left over from the push) rather than `infra/`, where `cdk.json` actually lives -- flagged and corrected before the deploy ran. |
| 2026-08-30 18:51 EDT | `7e74547`..`176f26b` | **Mobile capability-slider redesign + maturity-curve formula/data fixes -- pushed to GitHub, NOT yet deployed.** Ben ran `git push origin main` from his own terminal: `7e74547..176f26b`, 23 objects (12 new), 10.29 KiB, clean. Covers Open Items #18 (mobile-only tick-label/badge redesign for the assessment sliders) and #19 (maturity curve's "Recommended" mark now `min(5, max(3, ceil(your_score)+1))` instead of a fixed 3.6; "Peer Leaders" figures refreshed with Ben's updated per-capability numbers). App-side Python/template changes only, no infra/dependency/bundling change -- needs a `cdk deploy PmtcApp` to go live, not yet run as of this push. |
| 2026-08-30 13:31-13:52 EDT | `cdk deploy PmtcApp` (x2) | **Flask-compress deploy broke the live site, reverted the same afternoon.** 13:31 EDT deploy shipped `flask_compress.Compress(app)` + `Flask-Compress==1.24` (Open Item #13) -- deploy itself reported clean, but the live site 500'd on every route (`Runtime.ImportModuleError: No module named 'backports'`). Reverted `app/app/__init__.py` and both requirements files to their exact prior content and redeployed at 13:52 EDT; Ben confirmed the site working again. Not committed at any point during the outage -- both the broken and reverted states existed only in the working tree. See CLAUDE_problems.md P059 for the full RCA. |
| 2026-08-28 21:33 EDT | `cdk deploy PmtcApp` | Redeployed with the merged email-delivery wiring on top (`d5ac5a5`) -- app-side half of Open Item #14 closed: `lambda:InvokeFunction` grant and `REPORT_MAILER_FUNCTION` env var are now live, so the app can actually call the mailer for real when a visitor requests a report. |
| 2026-08-30 02:22 EDT | `51410a0` | **Output Report deck reconciliation and Skywalker Industries wiring, committed and pushed by Ben from his own machine.** Pushed `2158328..51410a0`, 67 files (34 through Git LFS -- `output_report/` gained its own `.gitattributes` matching `app/`'s convention, so its PNGs/fonts/the new printed PDF go through LFS instead of bloating history). Commit staged from the bridge shell for the first time this project (git-lfs installed there this session, resolving the P033/P048 root cause), with Ben's explicit go-ahead on both the local-only git identity and the commit itself; push still ran from Ben's own machine since the bridge shell has no GitHub credentials. Contains: all 11 Output Report pages reconciled to test1's real `run_calculation()` output, verified against the live app's actual Jinja2+JS-executed rendering (one real bug found and fixed -- page 4's legend), then re-reconciled a second time onto Skywalker Industries, LLC as the deck's permanent illustrative scenario, re-verified with zero further discrepancies. Also: `DATA_CONSISTENCY_AUDIT.md`, `DATA_CONSISTENCY_FINDINGS.md`, `scenarios/skywalker_industries.json`, `test_runs/test{1,2,3}_result.json` added; page 12 (hero stats) formally cancelled in the tracking docs. Not deployed anywhere -- this is Output Report source content, not app code; nothing about the live `PmtcApp`/`PmtcMail`/`PmtcDomain` stacks changed or needs a redeploy from this commit. |
| 2026-08-30 13:11 EDT | `e997323`..`e0fb67c` | **Jinja2 output-report templates + standalone preview/QA tool, pushed live.** Ben ran `git push origin main` from his own terminal: `51410a0..e0fb67c`, 27 objects, clean. Two commits: `e997323` adds the 11 `output_report/*.tmpl.html` files (Jinja2-templatized deck, verified against 4 real scenarios), `tools/preview_report.py` (standalone QA script -- pulls a real visitor row from the Google Sheet, replays it through `calculator.run_calculation()`, renders the full deck, optional PDF export -- verified live against real Sheet rows, one real bug found and fixed along the way, P058), and a new top-level `.gitignore` (`tools/preview_output/`, `__pycache__/`); `e0fb67c` logs the commit itself in SESSION_LOG.md/PROJECT_STATE.md. Not deployed anywhere -- Output Report source content and a local dev tool only, no app/infra code changed, no redeploy needed. |
| 2026-08-30 14:07 EDT | `e0fb67c`..`71d482f` | **Documentation-only commit, pushed live.** Ben ran `git push origin main` from his own terminal: `e0fb67c..71d482f`, 6 objects, 9.79 KiB, clean. Logs the compaction-recovery pass, the Open Item #13 (flask-compress) build/outage/revert writeup, and CLAUDE_problems.md P059 -- no app/infra code changed (the flask-compress attempt itself was reverted before ever being committed, so neither its addition nor its removal appears in any commit). |
| 2026-08-30 14:20 EDT | `71d482f`..`7e74547` | **`goals`-in-mailer-payload fix, pushed live.** Ben ran `git push origin main` from his own terminal: `71d482f..7e74547`, 21 objects (11 new), 5.10 KiB, clean. `emailer.send_report()` now accepts an optional `goals` dict and forwards it alongside `results`/`lead` in the mailer invoke payload; `routes.py`'s one call site updated to pass `session.get('goals') or {}`. Closes the `results`/`goals` contract gap documented under Open Item #2 -- pure app-side Python, no dependency/bundling/infra change, nothing to redeploy (the mailer's own `handler.py` already ignores unknown payload keys harmlessly). Also carries the compaction-recovery log and the PROJECT_STATE.md registry fix from this same session. |
| 2026-08-28 19:53 EDT | `cdk deploy PmtcMail` | **Report delivery live.** New `PmtcMail` stack: container Lambda `pmtc-report-mailer` (LibreOffice on ARM64, 3008MB/180s, async-only, retries disabled), SES configuration set `pmtc-report-mail`, SNS topic for bounces/complaints/rejects. Deliberately declares no SES identity -- `geniusdrive.com` is already verified with production access in this account and belongs to `SmomaMail`. First attempt rolled back on an account guardrail (`Deny ses:* on configuration-set/ReportMail*`, tripped by a construct ID copied from the sibling project); fixed with an explicit `configurationSetName`, see CLAUDE_problems.md P052. Verified beyond the deploy output with a real end-to-end send: `{"ok": true}`, PDF converted in 3.9s (28,852 bytes, 368MB peak, 2.5s cold start), SES message id `010001a04ad06b1d-d8ddbc5c-...`, and the message confirmed delivered to the inbox rather than spam with `K1x Private Market Tax Capability Assessment.pdf` attached. Nothing about `PmtcApp` or `PmtcDomain` was touched. The app cannot yet call this function -- that needs a `PmtcApp` redeploy (Open Item #14b). Code not committed. |
| 2026-08-28 19:42 EDT | `73b4c4a` | Merge of `klutetr/new-domain` (custom domain) and this session's P051 doc-rename commit, pushed by Ben from his own machine. Conflict-resolved by hand across all 4 tracking docs (see SESSION_LOG.md 19:07 EDT entry) -- no app code touched by the resolution itself. |
| 2026-08-28 20:03 EDT | `20fcff8` | Open Item #12 (cookie fix) closed out for good -- committed as `2d2adf3` (fix) then `20fcff8` (doc closeout), pushed by Ben from his own machine. Deployed live and verified in a real cross-site iframe test before this commit. |
| 2026-08-28 17:37 EDT | `b917423` | All six pending fixes committed + pushed by Ben (breadcrumb nav, P047 Peer Leaders/rounding fix, `WORKBOOK.xlsx` refresh, narrative card, strengths/gaps fine-print relocation, maturity curve legend/labels). Not yet deployed. |
| 2026-08-28 17:47 EDT | `cdk deploy PmtcApp` | Redeployed Lambda with `b917423`'s code -- all six fixes now live. Same Function URL as the original 2026-08-28 deploy, no DNS/URL change on a code-only redeploy. |
| 2026-08-28 18:07 EDT | `cdk deploy PmtcApp` | Redeployed Lambda with `5d56eb4`'s code -- P049 fix (peer-eyebrow/narrative-card overlap) now live. Ben QA'd directly on the live site. |
| 2026-08-26 20:04 EDT | -- (no repo yet) | Workbook read and fully tiered; CLAUDE.md and this file filled from live workbook + wireframe + Ben's decisions. No code exists yet. |
| 2026-08-27 01:35 EDT | `c4996eb` | Phase 0 scaffold committed and pushed by Ben from his own machine: blueprint renamed to `pmtc`, `WORKBOOK.xlsx` added, `.env` created locally (not committed, gitignored). |
| 2026-08-27 02:25 EDT | `4a71a69` | Phases 2-4 committed and pushed by Ben from his own machine: `calculator.py`/`routes.py` rewritten, `profile.html`/`assessment.html`/`results.html` ported from the wireframe, `picture1.png` moved to `app/static/pmtc/`, 7 stale starter templates deleted. |
| 2026-08-26 22:33 EDT | `6dd77e6` | Phase 7 (auth removal) committed and pushed by Ben from his own machine: `app/auth.py` deleted, `app/__init__.py`/`requirements.txt`/`.env.example` stripped of Auth0 wiring (4 files changed, 33 deletions). `PROJECT_STATE.md`/`CLAUDE.md`/`SESSION_LOG.md` are outside this git repo (repo root is `Application/app/`, these live in the parent `Application/` folder) -- not committed, updated directly on disk. |
| 2026-08-26 22:44 EDT | `6990428` | Open Item #3 (check_files.sh suite rewrite) committed and pushed by Ben from his own machine: `check_files.sh`/`check_structure.py`/`check_css.py`/`check_js.py`/`check_routes.py` rewritten for this project's real structure, `.check_baseline.json` added (6 files changed, 268 insertions, 239 deletions). |
| 2026-08-27 EDT | `29c1c7c` | Results-page stat-tile update ported from wireframe `DESIGN_DECISIONS.md` §43/§44 (real sourced figures replacing placeholders, plus an inert internal sourcing JSON block), committed and pushed by Ben from his own machine (1 file changed, 63 insertions, 9 deletions). |
| 2026-08-27 22:10 EDT | `4109fea` | `calculator.py`'s `ARCHETYPE_BANDS` updated to 5 bands (added the new score-0 "Stuck in the Blocks" band, dropped the now-unneeded clamp) and given real per-archetype narrative text ported verbatim from the updated `K1x PMTC Assessment.xlsx` (`Data!B100:E104`) and cross-verified against wireframe `results.html`'s inert `archetype-reference-data` JSON block (`DESIGN_DECISIONS.md` §45). `ARCHETYPE_NARRATIVE_PLACEHOLDER` (lorem ipsum) removed. Verified byte-for-byte against the workbook and with a direct module-level sanity run (`run_calculation()` at score 0 and score 5) before commit. Committed and pushed by Ben from his own machine (1 file changed, 40 insertions, 21 deletions). |
| 2026-08-28 01:15 EDT | `fbe0958` | Phase 10 data capture committed and pushed by Ben from his own machine: `data_capture.py`/`routes.py` (schema/wiring) plus `requirements.txt` (`tzdata==2024.1` added, fixing the Windows `zoneinfo` crash found during live testing -- see CLAUDE_problems.md P042). 3 files changed, 192 insertions, 136 deletions. Confirmed via Ben's terminal output (`4109fea..fbe0958 main -> main`). Preceded by a full live end-to-end test on Ben's machine against real Google Sheets credentials -- all three data-capture paths (append, lead backfill, revise-and-resubmit update-in-place) confirmed nominal. |
| 2026-08-28 16:45 EDT | -- (not yet committed) | `app/WORKBOOK.xlsx` refreshed from Ben's latest `K1x PMTC Assessment.xlsx` (previously stale since 2026-08-26). Full cell-level diff against the prior copy run before making any code change. `calculator.py`'s `PEER_SCORES` fixed to source from `Data!D84:E96` ("PEER LEADERS") instead of `Data!B27:G39` ("Peer Comparison Data") -- see CLAUDE_problems.md P047. Added `_excel_round()` to match Excel's `ROUND()` behavior for the resulting `.x5` boundary case, applied to `your_score`/`peer_score`/gap deltas. Everything else the diff surfaced (goal 9's industry-conditional rename, the archetype band-0 addition, "10 dimensions/capabilities" copy, the `NR` schema) was already correctly implemented from earlier sessions -- confirmed unchanged, no further action needed. Not yet committed -- Ben to commit/push and redeploy. |
| 2026-08-28 17:15 EDT | `bc54c2b` (branch `klutetr/new-domain`, merged into `main` 2026-08-28) | **Custom domain live: `https://k1x-pmtc.geniusdrive.com/`.** New `PmtcDomain` CDK stack (CloudFront in front of the existing Lambda Function URL) deployed to `019163347448`/us-east-1, `CREATE_COMPLETE`. ACM certificate `8472dbb9-d9b9-4a1a-b3f7-b63ca58e804c` requested standalone and DNS-validated; two CNAMEs added at GoDaddy by hand (ACM validation record, then `k1x-pmtc` -> `d2gnzexdb6ddm8.cloudfront.net`). `PmtcApp` was not touched, redeployed, or re-keyed at any point -- `bin/app.ts` now creates each stack only when its own context is present, so the domain deploys from a machine holding none of the app's secrets. Verified end-to-end against the real hostname: correct TLS subject, http->https 301, `GET /` -> 302 to the custom host (no origin hostname leak), `POST /profile` -> 302, session round-trip confirmed with a cookie/no-cookie control pair, `x-cache: Miss` on every request, and no `x-frame-options` (required for the K1x iframe embed). `POST /assessment` deliberately not exercised -- it writes a real row to the live Google Sheet. One deploy failure and rollback along the way (CloudFront rejects cookie/compression parameters on a zero-TTL cache policy), fixed and logged as CLAUDE_problems.md P050; the cost is that pages are served uncompressed at ~65KB, recoverable via `flask-compress` at the origin. |

**Repo:** As of 2026-08-26/27, PMTC has its own dedicated repo -- `https://github.com/bspinsky-sketch/k1x-pmtc-assessment` -- migrated off the shared `web-project-starter` repo, which was reset back to a clean generic scaffold (see Key Decisions Log). `app/`'s `origin` remote now points at `k1x-pmtc-assessment`.
**Current authoritative Git commit:** `0a454d2` -- committed from the bridge shell with Ben's go-ahead and pushed by Ben from his own machine 2026-08-31 12:59 EDT (`1888a1c..0a454d2 main -> main`, confirmed success). Sets K1x's BCC recipients (`tiana.maher@k1x.io`, `scott.turner@k1x.io`) on every report email -- deployed and verified live via a real send's CloudWatch log before this push. Open Item #2/#17 (the real mailer itself) remains RESOLVED as of `1888a1c`; this commit is a small config follow-up, not a code change.
**Current live workbook version:** `K1x PMTC Assessment.xlsx`, updated again on Ben's machine 2026-08-28 (mtime 20:36 UTC). `app/WORKBOOK.xlsx` (the repo's own copy) refreshed to match as of 2026-08-28 16:45 EDT -- no longer stale. `K1x PMTC Assessment OLD1.xlsx` and `K1x PMTC Assessment copy 2.xlsx` in the project root are not the current version -- do not use.
