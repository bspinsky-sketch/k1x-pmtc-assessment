# K1x PMTC Assessment -- Project Reference

**Purpose:** Authoritative project reference for Claude. Read at every session start before any substantive work.

**Last updated:** 2026-08-30 02:45 EDT (Output Report pages converted to real Jinja2 templates and verified against 4 real scenarios -- test1/test2/test3/Skywalker -- Open Item #2, in progress, not yet wired into any live delivery path. See new Key Decisions Log entry and SESSION_LOG.md.)

---

## EFFICIENCY DIRECTIVE -- STANDING RULE

Efficiency is a primary objective in all work.

- **Changes Claude can make:** Advise Ben first, then implement immediately unless told otherwise.
- **Changes Ben needs to make:** Advise clearly -- what to change, why, and how.
- Minimize round-trips. Batch related changes. Don't ask for confirmation that can reasonably be inferred.
- **Ben will name the file and approximate location when flagging an issue.** Go straight to the fix.
- **Explicit confirmation is required before proceeding on any plan.** Firm standing rule.
- **Calibrate confidence.** Flag uncertainty explicitly. Do not state compaction reconstructions as facts.
- **No em dashes** -- use en dashes (--) or restructure.
- **No multiple-choice question pickers.** Ask in plain text. Ben's answers are always free text.

---

## SESSION-START PROTOCOL -- MANDATORY

Before any research, coding, or content work, Claude MUST:
1. Read `CLAUDE.md` (this file)
2. Read `PROJECT_STATE.md` for current open/closed status
3. Read `STANDING_RULES.md` -- required before any build operation
4. Read `CLAUDE_problems.md` for any pattern relevant to the current task
5. Read `PLATFORM.md` if about to begin any build operation
6. If working on scoring/weighting/peer-comparison logic, re-verify against the live workbook -- this model has changed shape at least once already (12 dimensions/10 goals down to 10 dimensions/6 goals, tracked via red-cell-fill marking inactive rows) and could change again.
7. Write/verify the session-start verification marker (`.session_protocol_verified`, same folder as this file) once steps 1-4 are confirmed done via actual tool calls this turn -- see STANDING_RULES.md's SESSION-START AND COMPACTION RECOVERY PROTOCOL section for the full mechanism.

---

## Project Overview

**Client:** K1x
**Deliverable:** Web-based Capability & Maturity Assessment ("PMTC" -- Private Market Tax Capability). Embedded (iframe) on a K1x-hosted site. Scores a firm across 10 K-1 processing/tax-workflow capability dimensions on a 0-5 maturity scale, compares against peer benchmarks, and surfaces goal-aligned priority gaps. A downloadable Output Report (HTML-to-PDF, not PPTX -- see Key Decisions Log) is planned; a full 12-page mockup already exists (dummy scenario, built and delivered 2026-08-28) but isn't wired into the live app yet -- see Open Items.
**Primary users:** Prospective/current K1x clients (accounting firms, family offices, financial institutions, funds, tax-exempt orgs) fill out the tool anonymously; K1x sales/CS and the firm's own staff read the Results output. Lead capture (name/company/email) happens at the "Get My Report" step, not at intake.
**Model:** Profile (org name, industry, 6 goal priorities) -> Assessment (10 capability ratings, None/Ad-Hoc/Standardized/Automated/Integrated/Autonomous = 0-5) -> Calculation (per-capability score, overall average, peer comparison, goal-weighted priority-gap ranking) -> Results (score ring, maturity curve, strengths/opportunities, lead-capture modal) -> [future phase] Output Report (HTML-to-PDF).

**Note on scope vs. the template's usual shape:** this project has no dollar-denominated ROI/investment model, no CODN, no benefit calculators. It is a pure capability-maturity scoring and peer-benchmarking tool. Sections below that reference "benefits" or "investment" in the standard template are marked N/A.

---

## File Locations

All files in: `C:\Users\Ben\Documents\GENIUS DRIVE\GD Projects\K1x\PMTC assessment\`

| File | Purpose | Status |
|------|---------|--------|
| `Application\CLAUDE.md` | This file | Maintain actively |
| `Application\CLAUDE_problems.md` | Failure patterns and mitigations (shared template, no K1x-specific entries yet) | Maintain actively |
| `Application\PROJECT_STATE.md` | Running state: open items, decisions log | Maintain actively |
| `Application\SESSION_LOG.md` | Timestamped session log | Maintain actively |
| `Application\STANDING_RULES.md` | Behavioral rules | Read-only reference |
| `Application\PLATFORM.md` | Web stack patterns | Read-only reference |
| `K1x PMTC Assessment.xlsx` | Source workbook -- source of truth for all model logic | Reference -- do not modify |
| `K1x PMTC Assessment OLD1.xlsx` | Prior workbook version, kept alongside for reference | Do not use -- superseded |
| `wireframe\profile.html`, `assessment.html`, `results.html` | Approved (95% -- pending final client sign-off) static wireframe. Production templates in Phase 3 should match these exactly. | Reference -- do not modify directly; port into Jinja2 |
| `wireframe\DESIGN_DECISIONS.md` | Full decision log for the wireframe. Read before touching layout/copy/interaction. | Reference |
| `Application/output_report/` | The real, current Output Report build, page by page. **Superseded the old dummy-scenario mockup below after a real design review found it read as generic, not K1x's own material.** | **11 of 11 pages locked, reordered 2026-08-29 per Ben, Page 12 cancelled 2026-08-30 -- the deck is an 11-page final. Data-consistency reconciliation complete: verified against the live app's real rendering, now permanently wired to the Skywalker Industries, LLC scenario -- see Key Decisions Log.** Current order: `01-cover.html`, `02-goals.html`, `03-how-scored.html`, `04-capability.html`, `05-where-you-stand.html`, `06-solutions.html`, `07/08/09-roadmap.html` (roadmap parts 1-3), `10-success-story.html`, `11-trust.html` -- methodology now runs right after goals, ahead of both score views, and the two trust/proof pages moved to the back of the deck to close it out. `DECK_TRACKER.html` reflects the current order and is updated whenever a page locks or moves. See "Output Report Slide Map" below and SESSION_LOG.md for the full per-page build history and design techniques (x-axis widening, template-plus-instance data architecture, auto-measured card heights, wave-background treatment). **This is the template `mailer/generate.py` needs**; until it is finished and in the repo, the mailer renders a placeholder of its own. |
| `Application\app\` | Flask application (Phase 2+) | Built -- Phases 2/3/4/10 complete |
| `Application\infra\` | CDK deploy code: three independent stacks (`PmtcApp`, `PmtcDomain`, `PmtcMail`) | Built. `cdk.json` is gitignored (real secrets); `cdk.example.json` is the committed template |
| `Application\mailer\` | The report mailer: container Lambda, SES, currently LibreOffice + python-pptx. See its own `README.md` | **Live end to end, `PmtcApp` included** (deployed 2026-08-28, verified with a real send and inbox delivery). Renders a placeholder single-slide deck with the visitor's real computed figures -- not yet the actual Output Report HTML, since that mockup isn't in the repo yet. Conversion from PPTX/LibreOffice to the agreed HTML-to-PDF/Chromium pipeline is open and in progress (Open Item #17) |

---

## Workbook Structure (source of truth, verified 2026-08-26)

**Sheets:** `Profile`, `Assessment`, `Results`, `Data`. (`Sheet1` exists but is an unused stray content-revision note table -- ignore it, nothing references it.)

**Inactive-row convention:** the workbook marks retired rows with solid red fill (`FFFF0000`) rather than deleting them, consistently across `Profile`, `Assessment`, and `Data`. Confirmed 2026-08-26 across the whole workbook (not just Assessment) -- see Key Decisions Log.

**Calc-engine tier: Tier 1 (hand-port to Python).** Every formula in the workbook is VLOOKUP/HLOOKUP/RANK.EQ/COUNTIF/XLOOKUP/AVERAGE/IF -- no LAMBDA, no volatile functions, no spilling dynamic arrays. XLOOKUP appears (Results sheet) but is used as a plain scalar lookup, not a spill. Small enough that Tier 2 (xlcalculator) isn't worth the dependency.

---

## Capability Dimensions (10 active, in workbook row order)

Replaces the template's "Challenge Areas" table -- this project scores capabilities directly rather than gating benefits behind challenge ratings.

| # | Capability | Assessment row | Named ranges (`_lvl` / `_desc` / `_advice`) |
|---|------------|----------------|----------------------------------------------|
| D1 | Document Intake | 9 | `Assess_DocumentIntake_lvl/desc/advice` |
| D2 | Inventory Management | 10 | `Assess_InventoryManagement_lvl/desc/advice` |
| D3 | Data Extraction | 11 | `Assess_DataExtraction_lvl/desc/advice` |
| D4 | Data Validation | 12 | `Assess_DataValidation_lvl/desc/advice` |
| D5 | Data Review | 13 | `Assess_DataReview_lvl/desc/advice` |
| D6 | Tax Analysis & Reporting | 14 | `Assess_TaxAnalysis_Reporting_lvl/desc/advice` |
| D7 | Integration | 15 | `Assess_Integration_lvl/desc/advice` |
| D8 | Resource Structure | 16 | `Assess_ResourceStructure_lvl/desc/advice` |
| D9 | Advisory | 17 | `Assess_Advisory_lvl/desc/advice` |
| D10 | Governance & Trust | 18 | `Assess_Governance_Trust_lvl/desc/advice` |

**Retired (red-filled, do not port):** D11 Adoption Readiness (row 19), D12 Value Realization (row 20). Named ranges still exist for these (`Assess_AdoptionReadiness_*`, `Assess_ValueRealization_*`) but they are out of scope.

**Scale (all 10 dimensions, `Data!B19:C24`):** None=0, Ad-hoc=1, Standardized=2, Automated=3, Integrated=4, Autonomous=5. None is a real, valid, fully-weighted rating -- there is no N/A / exclude concept.

**Overall score:** average of the 10 active dimensions' scores, rounded to 1 decimal. (The live workbook's own `Assessment!N21` formula averages all 12 rows including the 2 retired ones -- do not copy that formula as-is; the Python port must average only the 10 active rows. Same issue in `Data!E97`, the peer-score average -- see Key Decisions Log.)

---

## Goals (6 active, Profile sheet)

| # | Goal | Section | Named range (priority input) | Weight named range |
|---|------|---------|-------------------------------|---------------------|
| G1 | Reduce Cycle Time | Evolve Capacity | `Goal_reduceTime` | `GW_1` |
| G2 | Centralize & Standardize | Evolve Capacity | `Goal_standardize` | `GW_2` |
| G3 | Support Scalable Growth | Evolve Capacity | `Goal_scalableGrowth` | `GW_5` |
| G4 | Improve Accuracy | Evolve Confidence | `Goal_accuracy` | `GW_6` |
| G5 | Elevate Client Experience | Evolve Confidence | `Goal_clientExperience` | `GW_7` |
| G6 | Goal 9 (dynamic) -- "Expand Advisory Services" if Industry=Accounting, else "Elevate Decision Support" | Evolve Capability | `Goal_advisorySvcs` (label/desc driven by `Goal9_lbl`/`Goal9_desc` formulas keyed on `Industry`) | `GW_9` |

Priority scale (`Data!B12:C16`): None=0, Low=1, Medium=2, High=3, Top=4.

**Retired (red-filled, do not port):** Strengthen Integration (`GW_3`), Reduce Total Cost (`GW_4`), Drive Team Adoption (`GW_8`), Expand Capabilities (`GW_10`). These weight ranges still exist in the workbook and resolve to 0 -- safe to leave wired but always 0 if easier than stripping them out.

**Validation (per wireframe, confirmed live):** all 6 goals must be ranked (any level, including all-None) to proceed from Profile. There is no longer a "at least one High/Top" floor -- that was dropped in wireframe decision §33.

---

## Goal-to-Dimension Weighting Model (drives the priority-gap ranking)

Each of the 10 active dimensions' "weighted gap" score (`Assessment!U` column) sums the priority weight (`GW_n`) of every goal that maps to it: **x2 if the dimension is that goal's primary capability, x1 if supporting** (per `Data!B43:E56`), then adds the dimension's own strength rank (`S` column -- rank 1 = highest raw score) as a way of biasing toward dimensions that are both goal-relevant and currently weak. The `T` column then ranks all 10 dimensions by that composite `U` value; Results pulls the top 3 as "priority areas to improve" and the top 3 by raw score (`S` column) as "where you scored highest." This logic isn't written up anywhere else -- confirmed by reading the formulas directly, not documented by the client. Re-verify against the live workbook if goal or dimension counts change again.

---

## Investment Model

**N/A.** No dollar-denominated ROI/investment model exists in this workbook. Do not add one without an explicit request -- this is a maturity-assessment tool, not a business-value calculator.

---

## Output Report Slide Map

**Built once, paused.** A real 12-page slide map exists as an HTML+PDF mockup (dummy "Meridian Fund Services" scenario, uniform 1280x720 pages, K1x's own Evolve Capacity/Confidence/Capability framework as the connective thread, real curve chart and bar chart ported directly from `results.html`'s own JS/CSS) -- built and delivered to Ben 2026-08-28 12:46-13:05 EDT, not saved in the project folder (conversation-only, see SESSION_LOG.md for the full build history). Format is HTML-to-PDF via Playwright/headless Chromium, not PPTX. Ben paused ("put a pin") to prioritize hosting; the live tool has since deployed with its current interim "Get My Report" modal (captures lead info, promises an emailed report, sends nothing yet -- Q3/email delivery still open). Next steps when this resumes: wire the mockup's real design/layout to actual per-user session data instead of the dummy scenario, and resolve Q3 so something can actually be sent. Two known issues flagged when the mockup was built, not yet fixed: the maturity-curve page's Your-Score/Peer-Leaders marks can sit on top of each other when they're numerically equal (a data-coincidence, not a bug -- the curve component has no collision handling for it); the testimonials slide had font-substitution overlap artifacts in the build sandbox (`Outfit`/`Play` weren't installed/fetchable there) -- fix is for Ben to export that one slide to PNG from PowerPoint on his own machine.

**Superseded 2026-08-28 evening.** A design-review pass compared this mockup directly against K1x's own real marketing decks and a comparable third-party maturity report (Mass Group's Operational Integration Readiness Report) rather than judging it in isolation, and it did not hold up: no real K1x logo anywhere on the cover, flat single-color backgrounds where K1x's own material is always textured/glowing, a title and subhead that undersold both the client and the assessment's real 10-capability breadth (it measures the whole private-market tax operation -- intake through advisory and governance -- not just K-1 processing), and the testimonials font-substitution bug above turned out to have never actually been disclosed to Ben when the file was delivered, despite this section's own earlier claim that it was. The font-substitution root cause was also found and fixed that same evening: the correct fonts were embedded in the source deck the whole time (PowerPoint's obfuscated `.fntdata` container, not fetchable from Google Fonts/GitHub as first assumed); the real Outfit family is now saved at `K1x Design System Integration/fonts/Outfit/` for any future export. Rather than patch the old mockup, the design restarted page by page at `Application/output_report/`. Page 1 (cover) is locked in -- see that folder's own file-reference row above. The maturity-curve collision issue above is unrelated to any of this and remains unfixed.

---

## Key Decisions Log

| Decision | Rationale |
|----------|-----------|
| Calc engine: Tier 1 (hand-port to Python) | Workbook has no LAMBDA/dynamic-array/volatile formulas; simple enough that xlcalculator's dependency isn't worth it. Confirmed 2026-08-26. |
| 10 active capability dimensions, not 12 | Workbook marks Adoption Readiness and Value Realization red-filled/inactive, consistently across Profile, Assessment, Data, and Results staging rows. Matches the wireframe's `CAPS` array (already built to 10) exactly. Confirmed with Ben 2026-08-26/27. |
| 6 active goals, not 10 | Same red-fill convention retires Strengthen Integration, Reduce Total Cost, Drive Team Adoption, Expand Capabilities. Matches wireframe's `GOALS` array. Confirmed 2026-08-26/27. |
| None = literal 0, no N/A/exclude concept | Ben confirmed 2026-08-27: "None" is a valid, real rating that scores 0 and is averaged in normally. This resolves all of the parked questions in the old `OPEN_QUESTIONS.md` (denominator adjustment, N/A chart rendering, N/A in the gap list, minimum-rated floor) -- none of them apply anymore. That file is fully resolved as of this decision. |
| Peer comparison data source -- **SUPERSEDED 2026-08-28, see CLAUDE_problems.md P047** | Original 2026-08-27 decision (below, struck through) conflated two different tables. There are actually two separate peer-data tables on `Data`: `B27:G39` ("Peer Comparison Data," 5 industry columns, all currently identical seed values) and `D84:E96` (a single column literally headed "PEER LEADERS"). Only the second is ever actually referenced by a live formula for a *score* -- `Results!F7`/`R_peerScore` is `=Data!E97`, which averages `D84:E96`'s 10 active rows. `B27:G39`'s per-capability score columns (rows 28-39) are never referenced by any formula anywhere in the workbook; only its row 40 (Peer Count) is, via `Results!F9`'s HLOOKUP. `calculator.py`'s `PEER_SCORES` had been ported from `B27:G39` (giving a peer score of 1.9) instead of `D84:E96` (the real source, 2.6) -- found when Ben flagged the displayed Peer Leaders number "doesn't look right." Fixed 2026-08-28: `PEER_SCORES` now sourced from `D84:E96`; `PEER_COUNTS` unchanged (still correctly sourced from `B27:G40` row 40, the one part of that table that's actually live-wired). Re-verified against Ben's 2026-08-28 refreshed workbook -- both tables unchanged from the version this was first (mis-)ported from. ~~Ben confirmed 2026-08-27 these are seed/placeholder values, "as complete as it can be until we get actual data rolling in." Not a bug to chase -- implement faithfully as-is (including that all 5 industries currently show identical peer averages), swap in real data later via a normal workbook refresh (see `modules/workbook_lifecycle.md`), no code change needed.~~ That confirmation is still accurate for what it was actually about (the *industry-segmented* seed data, `B27:G39`) -- it just wasn't the table feeding the displayed score. |
| Total-score and peer-score averages -- **CORRECTED 2026-08-28** | Original 2026-08-27 claim (that `Assessment!N21` and `Data!E97` both average across all 12 rows including the 2 retired ones) was wrong -- re-checked directly against the live formulas while investigating P047: `N21` is `=ROUND(AVERAGE(N8:N18),1)` (rows 19-20, where the 2 retired capabilities' scores live, are outside that range) and `E97` is `=ROUND(AVERAGE(E85:E94),1)` (same story -- rows 95-96 are outside it). Both workbook formulas already correctly average only the 10 active rows; no 12-row contamination exists. This doesn't change any code -- `calculator.py`'s Python port already independently averages only the 10 active `CAPABILITY_KEYS`, which was already correct and still is -- it just corrects the *reason* given for that choice. Also surfaced in the same investigation: both formulas wrap their `AVERAGE()` in `ROUND(...,1)`, which Python's built-in `round()` does not reliably match for a sum of decimal (non-integer) inputs landing exactly on a .x5 boundary (`round(2.55, 1)` is `2.5` in Python, `ROUND(2.55,1)` is `2.6` in Excel) -- see `_excel_round()` in `calculator.py` and CLAUDE_problems.md P047, added to guard `your_score`, `peer_score`, and the per-capability gap deltas against this. |
| PPTX report generation deferred to its own phase -- **SUPERSEDED 2026-08-28** | Superseded on two counts: the format was never actually PPTX (HTML-to-PDF was the real agreement, prototyped 2026-08-28 12:46-13:05 EDT as a 12-page mockup -- see "Output Report Slide Map" above), and a template now exists (that mockup), it's just paused rather than blocked. Original sequencing logic (build Phases 0-4 first) still held and is why this was paused, not abandoned. Renamed Open Item #2/Phase 5/9 from "PPTX report" to "Output Report" per Ben 2026-08-28, to stop the docs describing a format that was never actually the plan. |
| Archetype band clamped at score 1 for scores below it -- **SUPERSEDED 2026-08-28** | Original 2026-08-27 decision, superseded once the workbook gained a real fifth band. `Data!B100:E104` now has an explicit score-0 band ("Stuck in the Blocks" / "Value is Unrealized") plus a real descriptive paragraph per band (column E, new). The Python port (`calculator.py`) now carries all 5 bands with their real narrative text (verified byte-for-byte against the workbook and against the matching inert JSON block in wireframe `results.html`, wireframe `DESIGN_DECISIONS.md` §45) and no longer needs the clamp -- band 0 covers the full range with no gap. The old lorem-ipsum `ARCHETYPE_NARRATIVE_PLACEHOLDER` is gone. |
| Archetype narrative text ported into `calculator.py` verbatim from the workbook / wireframe §45 | Five per-archetype paragraphs (previously nonexistent -- the wireframe used lorem ipsum, per the superseded decision above) now live in `Data!E100:E104` and in `calculator.py`'s `ARCHETYPE_BANDS`. Deliberately not ported into production `results.html` as a matching inert reference block the way §44's stat-tile sourcing was -- that block's stated purpose (holding copy for a future score-based band selection that had not been built yet) is fully satisfied once the live app actually does the score-based selection, so an inert duplicate would be redundant. 2026-08-28. |
| `Assess_*_advice` (per-capability, per-level "what to do next" text, `Data!B68:H80`) not wired into calculator.py or the three pages | Not displayed anywhere in the approved wireframe -- it's report content (`Q` column / named ranges), and the Output Report phase is paused (Open Item #2) rather than blocked -- a template/mockup already exists, it's just not wired to live data yet. Revisit when that phase resumes; the underlying `Data!B68:H80` table is fully read and documented in this session's transcript if it's needed sooner. 2026-08-27.
| Lead-capture (`POST /api/lead`) is a stub -- accepts and acknowledges, persists nothing | Data-capture method (Q2) and email delivery (Q3) are both still unconfirmed defaults in PROJECT_STATE.md. Wiring the modal to a real endpoint (rather than leaving it pure client-side, as the wireframe has it) means the UI is ready the moment Ben confirms Q2/Q3 -- only `lead_capture()` in routes.py needs to change, not the templates. 2026-08-27.
| Re-verified 10-active-capability model after Ben briefly live-tested a 12-capability variant in the workbook, then reverted it | On 2026-08-27, Ben edited the live `K1x PMTC Assessment.xlsx` (not the repo's `WORKBOOK.xlsx` copy) to make `Assessment!N19`/`N20` live `VLOOKUP` formulas (previously hardcoded `0`) and widen `Assessment!N21` and `Data!E97` to average all 12 rows instead of 10 -- effectively trialing Adoption Readiness and Value Realization as live capabilities. Both rows were still red-filled (retired) throughout, and no UI question exists for either in the approved wireframe. Ben then reverted all four formula edits back to match the repo's `WORKBOOK.xlsx` baseline exactly. The only surviving diff is `Assessment!E20` ('None' -> 'Ad-hoc'), a demo/current-state value with zero computational effect since `N20` is still hardcoded to `0` and excluded from every live formula. Net result: no scope change, no calculator.py change -- the existing 10-capability decision (row above) and the re-derive-don't-copy decision stand as-is. No further action needed unless Ben asks to formally reactivate these two dimensions (which would need new Assessment-page question cards, not just a workbook formula change).
| No authentication -- Auth0 scaffold removed, not left disabled | Ben confirmed 2026-08-26/27 this is a public tool with no login. Rather than leave the starter's `AUTH_REQUIRED=False` default in place, removed it entirely: deleted `app/auth.py`, the `AUTH_REQUIRED`/`AUTH0_DOMAIN`/`AUTH0_CLIENT_ID`/`AUTH0_CLIENT_SECRET`/`AUTH0_CALLBACK_URL` config lines and `before_request` hook registration from `app/__init__.py`, the `authlib` line from `requirements.txt`, and the Auth0 block from `.env.example`/`.env`. Note: the scaffold's `before_request_hook()` referenced `url_for('auth.login')` but no `auth` blueprint was ever defined anywhere in the starter -- it would have raised `BuildError` if `AUTH_REQUIRED` were ever flipped to `True`, so it was dead/non-functional scaffolding, not a working feature being turned off. Verified the app factory still boots and all routes resolve via the Flask test client after removal.

| Data capture (Phase 10 / Q2) schema and architecture -- resolved 2026-08-28 | Ben set up the target Google Sheet himself and defined the exact column schema directly in the workbook (`NR!G2:AM3`): row 2 is the friendly header label, row 3 is the source named range (or blank for app-generated fields), and it maps 1:1 onto the Sheet's own columns with a fixed 6-column offset -- workbook `G` (a labels-only column, never written to) is Sheet `A`; workbook `H` (Timestamp) is Sheet `B`; every column after that shifts the same 6. Data starts at Sheet row 4. Final 33-column schema: Timestamp (ET, `YYYY-MM-DD HH:MM:SS`, generated at write time -- not a workbook field), Company, Industry, 6 goal priority scores (`GW_1/2/5/6/7/9`, one per active goal -- Ben simplified away the earlier draft's separate text-label columns, numeric score only), 10 capability levels (`Assess_*_lvl`, in the same order `calculator.py` already uses internally -- document_intake first), Your score, Peer score, Number of peers, top-3 strengths, top-3 gaps (capability names), then First Name/Last Name/Email/Opt-In. Timestamp uses Python's `zoneinfo` (`America/New_York`) rather than a fixed UTC offset, to handle the EST/EDT transition correctly automatically. Row lifecycle: a row is appended the first time a session reaches Results (`POST /assessment`, right when `run_calculation()` produces results -- not `GET /results`, since a GET can refire on reload/back-navigation and a POST only fires on a real submission); if the user goes back, revises an answer, and resubmits, the same row's assessment columns are updated in place rather than a second row being appended (`session['capture_row']` correlates the two); the "Get My Report" modal (`POST /api/lead`) backfills the four lead columns into that same row, with a defensive fallback that retries the initial append if `capture_row` was never set (e.g. Sheets was unreachable when the assessment was submitted). Deliberately not carried forward from the old ITSM-shaped stub module: notification-email-on-append, since email delivery (Q3) is explicitly out of scope for this phase per Ben. `data_capture.py` rewritten from scratch against this schema; `routes.py` wired at both trigger points; verified with a mocked-Sheets test harness (no real credentials exist in this environment) covering column order, append-vs-update ranges, row-number parsing, and graceful no-op when unconfigured. See PROJECT_STATE.md's Q2 row and Open Item #7. |
| Hosting platform: AWS (confirmed 2026-08-28) | Ben confirmed this project will host on AWS, resolving Scoping Decision Q1 (was defaulting to Google Cloud Run per PLATFORM.md's template default, `modules/hosting_cloudrun.md`). No AWS hosting module exists yet in the shared template (`modules/` currently only has `hosting_cloudrun.md` and `hosting_render.md`) -- one will need to be authored once the specific AWS service is chosen. This does not block anything today: Phase 8 (hosting and deployment) is still Pending and Phases 0-4/7/10 built so far have no hosting-specific code in them (Tier 1 calc engine has no LibreOffice/subprocess dependency that would be hosting-RAM-sensitive the way P028 describes for the ITSM project). Specific AWS service (ECS/Fargate, App Runner, Elastic Beanstalk, EC2, Lambda+API Gateway) still open -- asked Ben directly. |
| Hosting platform narrowed to AWS Lambda (confirmed 2026-08-28) | Ben chose Lambda specifically, from the set of AWS service options the previous decision (above) left open. Implication for this Flask app: Lambda does not run a WSGI app natively -- will need an adapter (`awslambdaric` base image + a WSGI-to-Lambda shim, or a library like Zappa/Mangum) in front of `create_app()`, plus a Lambda Function URL or API Gateway for the public endpoint. The AWS handoff kit's `StaticSiteStack` (S3+CloudFront) does not apply here regardless of this choice, since this app has a real backend; CloudFront, if used at all, would sit in front of the Lambda only for a custom domain/cert, not for serving static content. Phase 8 (hosting and deployment) is still Pending -- nothing built yet. |

| Lambda hosting architecture -- implementation specifics (drafted 2026-08-28, not yet deployed) | Adapter: `asgiref.wsgi.WsgiToAsgi` wraps the existing `create_app()`, then `mangum.Mangum(asgi_app, lifespan="off")` wraps that -- Flask is WSGI, a Lambda Function URL needs ASGI, and a Function URL's event shape matches Mangum's default expected shape (API Gateway HTTP API v2) so no extra Mangum config is needed. Packaging: local-pip-first bundling (`pip3 install --platform manylinux2014_aarch64 --only-binary=:all: ...`) rather than requiring Docker like `handoff/infra`'s `MailStack` does -- works from Ben's Windows machine with no Docker daemon, only possible because `app/requirements-lambda.txt` (a new, Lambda-only, trimmed dependency list) is pure Python; falls back to Docker-based bundling automatically if the local install ever fails. `requirements-lambda.txt` deliberately excludes `gunicorn` (unneeded under Mangum), `matplotlib`/`python-pptx` (only used by `report.py`, confirmed via `grep` to be unimported/dead code today), and `Flask-Session` (listed in the main `requirements.txt` but never wired into `app/__init__.py`, confirmed in a prior session) -- re-enabling any of those features later requires adding the package back to this specific file. Two bugs caught and fixed via real `cdk synth` runs in a cloud-sandbox scratch project before anything was written to the real repo: an ESM `__dirname` `ReferenceError` (fixed with the standard `path.dirname(fileURLToPath(import.meta.url))` polyfill, made explicit rather than relying on `tsx`'s CJS-interop the way the existing handoff stacks implicitly do) and a `Number("")` empty-string-to-`0` coercion that silently produced a 0MB/0s Lambda (fixed in `infra/bin/app.ts` by checking the raw context string's truthiness before calling `Number()`, since this project's `cdk.json` convention leaves unset context values as `""` rather than deleting the key). Verified beyond `cdk synth`: a real simulated Lambda Function URL invocation of the actual `create_app()` and actual templates (staged from Ben's machine), confirming `GET /` -> 302 `/profile` and `GET /profile` render correctly with zero Flask app code changes. Secrets (`FLASK_SECRET_KEY`, `GOOGLE_CREDENTIALS_JSON`) are passed as plain Lambda environment variables for now, same as `MailStack`'s pattern -- flagged in `infra/lib/app-stack.ts`'s own prop comment as worth moving to Secrets Manager once this tool handles real client data rather than the current test Sheet. Files: `Application/infra/` (new CDK app: `lib/app-stack.ts`, `bin/app.ts`, `cdk.json`, `package.json`, `tsconfig.json`, `README.md`), `Application/app/lambda_handler.py`, `Application/app/requirements-lambda.txt`. Not yet deployed -- see PROJECT_STATE.md Phase 8 and SESSION_LOG.md for status. |

| Email delivery: AWS SES, on its own container Lambda, invoked asynchronously (Q3 resolved 2026-08-28) | Tristen confirmed the report is emailed as a PDF, same shape as the sibling SMOMA project. **SES rather than the template default of Gmail SMTP** (`modules/email_gmail_smtp.md`): from Lambda, SES is an IAM permission with no password to store, inject, leak or rotate, whereas an app password has to live somewhere, against the standing rule on plaintext secrets -- and Google's own docs say a Workspace account may not be able to create one at all, which is what pushed the sibling project onto a personal Gmail (the same governance smell as Open Item #8). **The queue-bound half was already paid for:** `geniusdrive.com` is a verified, DKIM-signed SES identity in this same account (`019163347448`/us-east-1) and the account already has production access (50,000/day) -- both verified live 2026-08-28, both done by `SmomaMail`. **Which is exactly why `PmtcMail` must not declare the identity:** an SES identity is unique per account and region, so a second `ses.EmailIdentity` for `geniusdrive.com` fails the deploy, and had it ever succeeded, tearing down `PmtcMail` would revoke Mass Group's sending too. `mail-stack.ts` uses the domain only as a string to build an ARN for an IAM policy; it does create its own configuration set and SNS topic, so the two tools' bounce feeds stay separate (note the shared identity carries SmomaMail's set as its *default*, so every PMTC send names its own explicitly via `REPORT_CONFIG_SET`). **A container Lambda, not the existing Flask function:** LibreOffice is the only faithful PPTX-to-PDF converter, does not fit a zip, and took 66s at 3008MB on the sibling project, against `PmtcApp`'s 512MB/15s. **Invoked through IAM, not a Function URL:** SMOMA needs a public URL and a shared token in its HTML because its front end is a static page; this tool has a Flask backend, so `PmtcApp`'s role invokes `pmtc-report-mailer` by name -- no public endpoint, no token in the page. **Asynchronous (`InvocationType='Event'`), and `emailer.py` swallows every exception:** the modal has already promised the report, the lead is already durable in the Google Sheet by a separate path, and a synchronous call would show a 504 for a report that was about to send perfectly. `retryAttempts: 0` plus the handler catching its own exceptions, so neither alone is load-bearing against a duplicate send. **The two-stack seam is a literal function name**, not a CDK cross-stack reference, so each stack still deploys from a machine holding only its own context -- the cost is that `MailStackProps.functionName` and `app-stack.ts`'s `REPORT_MAILER_FUNCTION` must agree and nothing enforces it. Addresses: from `reports@geniusdrive.com` (no mailbox, and needs none -- SES verifies the domain, never the mailbox), Reply-To and bounces to `tklute@geniusdrive.com`, blind copy empty pending K1x. Files: `mailer/` (new: `Dockerfile`, `handler.py`, `generate.py`, `fonts.conf`, `try_mailer.py`, `README.md`), `infra/lib/mail-stack.ts`, `infra/deploy-mail.sh`, `infra/check-mail.sh`, plus edits to `infra/bin/app.ts`, `infra/lib/app-stack.ts`, `app/app/blueprints/pmtc/emailer.py` (rewritten from the dead ITSM stub that imported `app.itsmbvf.report`) and `routes.py`. |

| The report deck shipped today is a placeholder that draws real figures, not an empty PDF or an HTML-only email (2026-08-28) | Ben is building the real PMTC report template (Open Item #2) while the delivery path was being built. The choice was what to send in the meantime. A plain HTML email would have been quicker and would have left every genuinely hard part unproven -- whether python-pptx output converts at all, how long conversion takes at this memory setting, whether the fonts resolve rather than falling back to Liberation Mono, whether a multi-megabyte attachment survives SES. So `generate.py` draws one slide from scratch in the tool's own palette and fills it with the visitor's real computed results, and everything around it is the final architecture running for real. Verified end to end in the actual ARM64 image before any deploy: 28KB PDF, fonts resolving to Carlito via `fonts.conf`, and the rendered page inspected rather than assumed (which caught one real defect -- python-pptx's default 0.1in text-box insets meant boxes placed at the same x did not share a left edge; `_textbox()` now zeroes all four insets so the geometry means what it says). The placeholder says so on the slide itself, not only in the covering email, because a PDF gets forwarded and read away from the message that carried it. Swapping in the real deck is a `COPY master.pptx` line (already written as a comment in the `Dockerfile`), a real fill function keeping the `generate(data, out_path)` signature so `handler.py` does not change, and deleting the placeholder line -- see `mailer/README.md`, "When the real deck arrives". |

| Design-critique methodology for Output Report pages: verify against the live K1x product, don't just improve on taste (2026-08-29 03:05 EDT) | Page 5 (trust/stats) went through two rounds. Round 1: Ben's feedback was open-ended ("it looks kind of beige, no pop -- your thoughts, ideas?"), and the response was two real rendered mockups (hero gold tile, all-gold numerals) generated from general design instinct plus one real precedent (the source sales-deck slide's own yellow-box treatment of its $175M stat). Reasonable, but self-referential -- nothing outside this one slide and this one PPTX was consulted. Round 2: Ben gave a materially different instruction -- go crawl the actual K1x website for ideas, and specifically pointed at an asset he'd already pulled (`K1x_Asset-10-1920x1063.png`, a generative wave-line graphic) as a lead worth chasing. That's a narrower, source-grounded ask, not a request for more taste. What changed in the response to match: (1) went to the real site instead of reasoning from memory or the sales deck alone -- and the first attempt (`k1x.com`) turned out to be a basketball apparel brand's domain, not K1x; caught via web search rather than assumed, before anything was built on the wrong reference; the real site is `k1x.io`. (2) Browsed multiple sections of the real site (not just the hero) and came back with concrete, citable patterns already in production there: a solid-black-tile stat block with oversized colored numerals (their homepage), an open white-space stat grid using only thin rule dividers with no fill at all (their "Proven Results" section), and the wave-line graphic recurring site-wide as a background texture confined to calm/empty zones, never under dense text. (3) Treated the user-supplied wave PNG as data to inspect, not just an asset to place -- checked its actual pixel/alpha channel before touching it, confirming it was true RGBA (not a flat image against white) so it could be recolored (gold-tint, ink-tint) by overwriting RGB while preserving the existing alpha, the same technique already proven on the page-4/page-5 logo recolors. (4) Built two new mockups (variantC: open-rule grid, no tile fill -- direct copy of the real site's own "Proven Results" structure; variantD: solid black tiles + gold numerals, direct copy of the real site's homepage stat block, plus the recolored wave graphic filling the page's previously-flagged 130px of dead bottom whitespace) where every deviation from the previous flat gray-panel design cites the specific real K1x section it came from, rather than being argued from general design principle. (5) Caught a real rendering bug the same way other bugs have been caught on this project -- not by eyeballing a screenshot and moving on, but by sampling actual pixel values in the rendered PNG when the wave texture appeared to not be showing, which surfaced a genuine CSS stacking-context issue (a negative z-index child of a `position:relative` element with no explicit z-index escapes to the parent's stacking context and can render behind that parent's own opaque background) -- fixed by giving `.page` an explicit `z-index:0` so it establishes its own stacking context. General takeaway for the remaining Output Report pages: when a page reads as visually flat or is being redesigned rather than merely produced, check the live k1x.io site for an existing real pattern before inventing one -- the site itself has already solved several of these exact problems (stat walls, dark/light section transitions, background texture placement) and citing its own precedent is more persuasive and more on-brand than a plausible-sounding original composition. Also worth remembering for any future asset drop: `k1x.com` is NOT this K1x -- always confirm `k1x.io`. |

| Standing rule: ask for an annotated screenshot before guessing at an alignment/positioning request (2026-08-29 16:36 EDT) | Surfaced while building Output Report page 6: Ben marked up a rendered screenshot with red boxes (grouping) and lines (exact shared coordinates) to specify how the "Your Score" and "Peer" blocks should line up, and confirmed afterward that this communicated the target far better than prose would have -- "align the numbers" in words could mean several things, a line drawn through both numbers can't be misread. Two things that kind of annotation still doesn't resolve on its own: the trade-off in *how* to reach the end state when the aligned elements have different natural sizes (shrink one vs. add space to the other), and the fact that hand-drawn box edges are approximate, not a literal pixel spec -- both still need a quick text exchange. Ben's explicit instruction: going forward, if a request describes aligning/positioning specific visual elements and no annotated screenshot was provided and the ask isn't already unambiguous, ask for one (or describe the target back in concrete terms) before implementing, rather than guessing and risking a redo. Full method (absolute-position "nudge" + Playwright `getBoundingClientRect()` measurement) written up in `output_report/DESIGN_NOTES.md` Section 8/8a; also drafted as a candidate Claude skill (`pixel-perfect-alignment`), not yet saved. |

| Output Report page 7 ("How This Is Scored"): widen a chart's x-axis without touching its y-axis by scaling the reference component's viewBox width and x-constants together, not the whole viewBox (2026-08-29 19:22 EDT) | Ben's ask, working from the v1/v2 mockup review: reuse the real `curveChart` component from `results.html`, but stretch the gap between its six ticks so five zone cards below it could line up with them and hold real narrative text -- without inflating the curve's own height, since simply widening the container would scale both axes together (an "image resize," in Ben's words). The component's viewBox width and its `CURVE.left`/`CURVE.right` x-positions were multiplied by the same scale factor `k = TARGET_WIDTH/REF_WIDTH`; `CURVE.top`/`CURVE.bottom` (the y-axis) were left untouched. Because the physical-size math (`toUnits = VBW/renderedWidth`) depends on the *ratio* of viewBox width to rendered width, keeping that ratio constant while both grow by `k` reproduces the exact same dot/stroke/label sizes and the exact same rendered height at any target width -- confirmed by measurement (343.8px unchanged) before touching anything else. Zone-card alignment to the widened ticks was done by measuring the curve's actual rendered tick0/tick5 pixel positions after render (`getBoundingClientRect()` + the viewBox transform) and setting the zone strip's padding to match exactly, rather than computing a percentage by hand and hoping it agreed -- landed within 0.04px on both ends. Also surfaced: the SVG's own bottom-padding formula (`b = Math.max(190, CURVE.bottom + coefficient*label)`) was silently floored at 190 regardless of the coefficient, so trimming the coefficient alone did nothing until the floor itself was lowered (190 to 180) and re-verified against the actual rendered position of the last tick label so nothing clipped. Full narrative text (real `Data!B100:E104` archetype paragraphs, not placeholders) fits in all five cards at this width; two content changes were dropped along the way after discussion -- dueling "You"/"Peer" pills per card (space conflict with card-width goals, and Recommended never gets a pill since it's a fixed model constant, not a per-client fact) and the chart's own legend row (marks are already labeled directly above each dot at this width, so the legend was pure redundancy). Cards 4-5 needed the same background-photo scrim already used on page 6's score/peer boxes, since they sit over the brightest part of the photo. |
| Output Report page 8 ("Solutions"): first genuinely data-driven page in the deck, built as a template-plus-instance pair so it can regenerate for any client (2026-08-29 21:33 EDT) | Mirrors the architecture already proven on page 7's `curveChart.render(state)`: a reusable lookup table, `CAPABILITY_FIXES`, covers all 10 active capability dimensions and is keyed exactly like `calculator.py`'s `CAPABILITY_KEYS` (`document_intake` through `governance_trust`); a small per-client array, `GAPS`, holds just this client's top-3 in the same shape as `run_calculation()`'s real `results.gaps` output (`{key, name, delta, note}`, verbatim from `06-where-you-stand.html`'s "Priority Areas to Improve" card so the two pages never disagree). A `renderGaps(gaps)` function joins the two client-side to build the three visible `.map-row` cards. To wire this page to a real session instead of the dummy scenario, only `GAPS` needs to change -- swap it for `results.gaps`, no other code moves. Product/fix assignments in `CAPABILITY_FIXES` are sourced from the workbook's "K1x Ecosystem Reference" (`Data!B58:E66`, product -> mapped capability dimensions) and its per-capability "Next Step" advice text (`Data!B68:F78`) -- not invented, and not just "any mapped product": where a dimension's mapped-product list overlapped (several dimensions list K1 Aggregator alongside a later-stage product), the product actually named as the differentiator in that dimension's own advice text was used. This caught one real error before it shipped: a first pass assigned "K1xChange Trust Layer" to Governance & Trust from the sales deck's own capability banner, but the workbook's real advice text for that row (`Data!B78`) names K1 Aggregator throughout ("governed source of truth," "system-captured audit trail," "run a mock audit using only K1Aggregator reports") and never mentions K1xChange -- corrected to K1 Aggregator® per Ben's own pointer to the workbook as source of truth. Product icons are real assets, not drawn from scratch: extracted via browser screenshot (not fetch/curl -- two attempts to pull raw/base64 SVG text via `javascript_tool` were blocked by the tool's own anti-exfiltration guardrail and were not routed around) from k1x.io's "Dark" icon set, alpha-masked to isolate the glyph from its circular crop, then recolored onto a filled circle in each product's own real brand color sampled directly off its live product-page hero: K1 Aggregator® `#11D0A7` (k1x.io/k1-aggregator), K1 Creator® `#B57AE2` (k1x.io/k1-creator), 990 Tracker® `#00CDFA` (k1x.io/990-tracker). K1xChange® has no standalone product page or colored-circle asset on the live site -- it only appears as one stage in the platform page's workflow diagram -- so its amber `#F59E0B` is Ben's own choice, not sourced from K1x, filling the one warm color the other three left open; flagged as such in the file's own code comments so a future maintainer doesn't mistake it for a sourced brand color. K1xCelerator and K1xInsights (the `resource_structure` and `advisory` rows) have no icon of any kind on the live site as of this build and fall back to text-only. Locked and committed to the repo as `output_report/08-solutions.html`, plus `assets/icons/product-aggregator-color.png` and `assets/icons/product-change-color.png` (the only two icons this client's actual top-3 gaps reference; the Creator/990-Tracker color variants exist in the same style but aren't wired to anything for this client). See PROJECT_STATE.md Open Item #2 and SESSION_LOG.md for the full build/iteration history (headline and lede rewrites, icon-on-colored-circle exploration, wave-background redesign). |
| Output Report pages 9-11 ("Your Roadmap to the Next Level"): a per-capability, per-level maturity roadmap, built as two master lookup tables joined against a per-session levels array (2026-08-29 23:09 EDT) | Ben's source of reference was Mass Group's Operational Integration Readiness Report (slides 22-29, one full slide per capability, all 6 levels' worth of "next step" text shown top to bottom). Ben's own design was deliberately simpler: one row per capability, showing only the current level's description and its specific next step, aiming for 2-3 slides total instead of 10. Two master lookup tables now live in `output_report/assets/roadmap_data.js`, generated directly off the workbook (never hand-typed): `CURRENT_DESCRIPTIONS` (10 capabilities x 6 level-description strings each, sourced `Assessment!G9:L18`, the same "Description of Selected Level" text pages 6/8 already use) and `NEXT_STEP_ADVICE` (10 x 6 bullet-list arrays, sourced `Data!C69:H78`, the table literally titled "Next Step - Advice to get to next level" -- confirmed by reading `Assessment!Q9:Q18`'s actual formula, `HLOOKUP(E9, Data!$B$68:$H$80, B9+1, FALSE)`, rather than trusting Ben's approximate remembered range of `B69:G78`; the real range runs `C:H`, six level columns None through Autonomous, with `B` holding only the capability name). Both tables are keyed to the same 10 `CAPABILITY_KEYS` used everywhere else in this deck. Per Ben's explicit instruction, no text is compacted or summarized anywhere -- every bullet from the workbook's advice table renders in full. Each of the three pages carries its own small `CAPABILITY_LEVELS` instance object (which level 0-5 each capability is at) -- exactly the same template-plus-instance split as pages 7 and 8: swap that one object for the live app's real `Assessment!N9:N18` scores and every description and advice list on all three pages re-renders correctly for that session, with no other code change. The Autonomous (level 5) case has no "next step" in the workbook (nowhere higher to go) -- Ben's copy for that case, "Sustain and extend," is shown instead of a bullet list. Capabilities split 3/4/3 across the three slides in workbook order (Document Intake/Inventory Management/Data Extraction; Data Validation/Data Review/Tax Analysis & Reporting/Integration; Resource Structure/Advisory/Governance & Trust). This build's illustrative `CAPABILITY_LEVELS` (Document Intake=Standardized, Inventory Management=Automated, Data Extraction=Automated, Data Validation=Standardized, Data Review=Standardized, Tax Analysis & Reporting=Automated, Integration=None, Resource Structure=Standardized, Advisory=Ad-Hoc, Governance & Trust=None) keeps the three values already locked by pages 6/8's narrative text and fills in the other seven for illustration only -- Ben explicitly deferred reconciling this against page 3's bar chart (which implies a different, lower set) and page 7's stated 3.0 average (which implies a higher one) to a separate follow-up, not part of this build. |

| Roadmap card layout: auto-measured section heights instead of a hardcoded worst-case, so "Next step" always breaks at the same y with no wasted space (2026-08-29 23:09 EDT) | Ben's requirement: every card on a slide must be the same total height, with the current-state/next-step divider landing at the same y on every card, however long that particular capability's text happens to be. A hardcoded pixel height sized to the true worst case across all 10 capabilities x 6 levels (Governance & Trust's longest description at 223 chars; Advisory's longest advice list at 5 bullets/706 chars, both measured directly, not guessed) works but wastes a lot of vertical space on any slide whose capabilities are lighter than that -- confirmed visually on the first pass, page 9 in particular looked sparse. Replaced with a runtime measurement: each page renders its own cards' top (name/pill/description) and bottom (next-step/bullets) sections with natural height first, waits on `document.fonts.ready` so web-font metrics are final, then measures the tallest `.cap-top` and tallest `.cap-bottom` actually present on that specific slide and locks every card on the slide to those two heights via `el.style.height`. This keeps the alignment guarantee exactly as strict, fits each slide to its own real content instead of a global worst case, and -- since it measures rather than assumes -- will size correctly for whatever levels a real future session actually lands on, not just this illustrative scenario. The card row is also vertically centered (`.grid-wrap{flex:1;display:flex;align-items:center}`) between the lede and footer rather than pinned to the top, so a lighter slide doesn't read as top-heavy with dead space below it -- Ben's own call was the equal-height requirement; the centering was this session's judgment call to finish the effect, flagged as such when shown. |

| Roadmap card visual treatment: reused page 8's own light/dark two-tone card language plus the pages 4/5/8 gold-wave background, rather than a new palette, to fix a repeat "looks beige" note (2026-08-29 23:09 EDT) | Ben flagged the first built version (flat `#FAFAF8` cards on plain white) as beige, the same note page 5 got during its own design-critique pass (see the 2026-08-29 03:05 EDT entry above) -- and the fix followed the same rule that entry established: reuse a real, already-verified pattern from elsewhere in this project rather than add color for its own sake. The "next step" half of each card now uses page 8's own solid `#0A0A0A` fix-card treatment (gold left border, gold section label, off-white bullet text), while the "where you are today" half stays on a plain white card -- the same light-panel-into-dark-panel contrast page 8 already proved, just restacked vertically instead of side by side. The page background switched from flat white to the `wave-gold-strong.png` treatment already used on pages 4, 5, and 8, with the same header scrim so the lede stays legible -- confined to the calm margins above and below the card row, never under the dense card text, per the site-sourced rule from the 2026-08-29 03:05 EDT entry. Nothing about the underlying data/architecture changed; this was a CSS-only pass. |
| Output Report reordered so the client's own diagnostic arc runs unbroken, with methodology first and all external proof pulled to the back next to the new closing page (2026-08-29/30) | Ben's original ask for page 12 was a "hero stats" close, sourced from the live app's own results-page stat tiles. Reviewing that against the deck as built surfaced two problems worth fixing together rather than separately: (1) page 4 ("Your Success Story") already carries real named-client proof stats, so porting the app's three platform-general benchmark stats onto page 12 as-is would read as repeating the same case a second time, once with client names and once without; (2) the deck's own order interrupted the client's diagnostic thread -- goals and capability score, then two proof pages, then finally the client's own results -- and separately put the scoring methodology (page 7) after both result views instead of before them. Ben's fix, confirmed across several short exchanges rather than guessed at once: move "How This Is Scored" to run right after "What You Told Us," ahead of both score views, so the reader learns the method before seeing either result; and move both proof pages ("Your Success Story," "Scaled. Trusted. Proven.") to the back of the deck, immediately before the still-unbuilt hero-stat page, so all three external-proof pages read as one deliberate closing sequence (named clients, then scale/trust numbers, then the aggregate benchmark) instead of being split apart and repeating each other from a distance. New order: `01-cover`, `02-goals`, `03-how-scored` (was 07), `04-capability` (was 03), `05-where-you-stand` (was 06), `06-solutions` (was 08), `07/08/09-roadmap` (was 09/10/11), `10-success-story` (was 04), `11-trust` (was 05), then the new page 12. Executed as a pure reorder: files renamed via a temp-prefix pass to avoid collisions in the overlapping number permutation, each file's own `page-footer` number updated to match its new position, no page content changed. `DECK_TRACKER.html` rebuilt in the new order; thumbnails reassigned to their moved files without regenerating (no visual content changed). Flags an existing, already-tracked issue rather than creating one: pages 4 and 5 in the new order (capability score, where-you-stand) now sit back to back, which is exactly the pair already flagged for the cross-page scenario-consistency follow-up (PROJECT_STATE.md Open Item #2) -- the reorder doesn't make that problem worse, but does mean a reader now hits both scoring views in one page-turn, worth keeping in mind whenever that consistency pass happens. |
| Output Report pages 6 and 7-9 had grown back an eyebrow and several em dashes; fixed and logged as a regression, not a new rule (2026-08-29/30) | Caught by Ben, not self-caught: "EYEBROWS grew back into the roadmap slides," followed by "those slides have em dashes too." The three roadmap pages (then 09-11, now 07-09) each carried a `.part-tag` div above the `.h1` styled exactly like the page-header eyebrow banned since the very first mockup review ("no eyebrows anywhere") and specifically reiterated for the cover; no other locked page has one. The same three pages' shared lede used `&mdash;`, and page 8/`06-solutions.html` had seven separate em dashes in its own lede and its `CAPABILITY_FIXES` copy -- both against `STANDING_RULES.md`'s plainly-stated Core Behavioral Rules, and both already the subject of a full incident writeup once this session (see CLAUDE_problems.md P053/P054). Fixed by removing the `.part-tag` element entirely and folding "Part N of 3" into the `.h1` text itself (matching each file's own `<title>` wording, so no information was lost), and by replacing every em dash with its en-dash equivalent, a mechanical substitution that changed no wording. Verified clean with a full-directory grep sweep across every page in `output_report/` for both patterns, not just the two pages Ben flagged -- `DECK_TRACKER.html`'s title also had one and was fixed the same pass. Deliberately left `05-where-you-stand.html`'s own `.eyebrow`/`.score-eyebrow`/`.peer-eyebrow` classes alone: those are a verbatim, previously-approved port of the live app's own result-page UI labels, not a decorative page-header kicker, and out of scope for this complaint. Full RCA: CLAUDE_problems.md P055. |
| The goal-weighted "gaps" list can rank a capability the client is already ahead of peers on as a "priority gap" -- confirmed present in the live app today, not just the deck, and deliberately deferred to a single fix point (2026-08-30 01:09 EDT) | Surfaced first as a judgment call while building the deck's test2/test3 synthetic scenarios (DATA_CONSISTENCY_FINDINGS.md finding #5): `run_calculation()`'s `gaps` always returns exactly 3 capabilities by goal-weighted rank, with no floor that checks whether the ranked capability's delta (`scores[k] - peer[k]`) is actually negative. test2 (all-Level-5) made this concrete -- every delta in its 3-item gaps list was positive (governance_trust +2.8, data_extraction +1.7, integration +2.8), meaning "most ahead of peers," not behind. Ben asked whether this almost certainly also shows up in the live app's own Results page, since if so it should be fixed once at the source rather than separately in the deck. Checked directly this session, not assumed: `routes.py` calls `run_calculation()` exactly once (line 116) and stores the full dict in `session['results']` with no gap-filtering; `results.html` loops `{% for g in results.gaps %}` under the "Priority areas to improve" / "Biggest gaps. Weighted to your goals." header with no filter either -- and its own CSS already has a `delta-ahead` class for a non-negative `g.delta` (`'delta-ahead' if g.delta >= 0 else 'delta-behind'`), meaning the template code already anticipates a gap landing on the "ahead" side without ever addressing the copy ("Biggest gaps") that goes with it. Confirms Ben's hypothesis: this is a real, live, single-sourced behavior today, not a deck-only artifact. Ben's call: leave it as-is on both sides for now, fix it once in `calculator.py`'s `run_calculation()` gap-selection logic shortly after the deck goes to production, so the live Results page and the deck (once wired to real session data) update together automatically rather than needing two coordinated fixes. That guarantee holds only as long as the deck's future live-wiring literally consumes `results.gaps` verbatim, as already documented for the Solutions page ("swap GAPS for results.gaps, no other code moves") -- worth re-confirming at that time that the wiring didn't drift into its own gap-filtering logic in the meantime. |
| Page 12 (hero stats, closing page) cancelled -- the Output Report deck is final at 11 pages (2026-08-30 01:15 EDT) | Ben's direct instruction: "There will be no page 12." The deck's current 11-page order (`01-cover` through `11-trust`, locked 2026-08-29/30 per the reorder above) now stands as the complete, final page set -- no further page needs designing or building. The hero-stat sourcing research done in preparation for page 12 (the live app's own results-page stat tiles -- 2.9x cycle time, 90% fewer touchpoints, 61% advisory revenue -- verified via `wireframe/DESIGN_DECISIONS.md` §39-44) stays on record as-is; it simply isn't going into this deck. Updated: this file's `output_report/` file-locations row and Output Report Slide Map framing, `PROJECT_STATE.md` Phase 5 and Open Item #2, `DATA_CONSISTENCY_AUDIT.md`'s out-of-scope table (row struck through, not deleted, so the audit's own history stays intact), and `output_report/DECK_TRACKER.html`'s "Still needed" section (now reads "Deck complete (11 of 11)"). Remaining Output Report work is unchanged in kind, just no longer includes a 12th page: cross-page data-consistency reconciliation (in progress via the test1/test2/test3 synthetic-scenario work) and wiring the finished pages to real per-user session data. |
| Output Report data-consistency reconciliation completed end to end: one real scenario per pass, verified against the live app, then a second real scenario wired in as the deck's permanent content (2026-08-30 01:50 EDT) | Executed per Ben's explicit sequencing instruction: "fix bugs, then run against the app & fix more bugs, then when you think all bugs are squashed, run Skywalker." Phase 1 (fix bugs): every one of the 11 pages' hardcoded values was replaced with test1's real `run_calculation()` output (industry Accounting, your_score 1.9) -- the same "pick one real scenario and make every page match it" strategy `DATA_CONSISTENCY_FINDINGS.md` itself recommended. Page 2 gained a real `SESSION_GOALS`/`GOAL9_BY_INDUSTRY` instance object (previously had none at all -- finding 6); pages 3-9 got value-only reconciliation onto the same dataset. Phase 2 (run against the app): built a two-layer verification rig in the cloud workspace -- a Flask test-client render of the real `create_app()`, seeded with `session['results']` (since `/results` needs only that one key), piped through a headless-Chromium Playwright pass so the page's client-side JS actually executes (`results.html` mixes Jinja2-rendered values with JS-computed ones, e.g. `#scoreRingNum` starts as a static placeholder in the markup and is overwritten by `YOUR_SCORE.toFixed(1)` at real load -- a JS-less render shows the stale placeholder and looks like a false bug). Checked score, band, peer stats, all strengths/gaps rows, all 10 capability-bar widths, the curve chart's real values, and page 2's `GOAL9_BY_INDUSTRY` diffed word-for-word against `profile.html`'s live source. One real bug found: `04-capability.html`'s legend said only "Peer leaders" where the live app shows "Peer leaders (Accounting, n=133)" -- fixed to match. Phase 3 (run Skywalker): with zero further discrepancies found, all 11 pages were reconciled a second time onto `scenarios/skywalker_industries.json`'s real output (industry Fund, your_score 1.8) -- Ben's own recurring real-world test company, confirmed earlier this session as "the new permanent template replacing Meridian everywhere once built." Re-ran the same Flask+Playwright rig against Skywalker's seeded results; every field matched the newly-wired deck exactly, including the three capability-bar rows whose values genuinely differ from test1 (Inventory Management, Data Extraction, Governance & Trust) and the three-item gaps list, which is now genuinely different in composition from test1's (Governance & Trust/Integration/Data Extraction vs. test1's Data Review/Integration/Governance & Trust) -- both scenarios independently verified end to end rather than one being assumed to generalize from the other. Full per-page edit detail in SESSION_LOG.md's 2026-08-30 entries. Net effect: the Output Report deck's remaining open item (PROJECT_STATE.md Open Item #2) is now narrowed to exactly one thing -- wiring the finished, internally-consistent, live-app-verified design to real per-user session data in place of the fixed Skywalker Industries scenario. **Also logged this session, unrelated to the deck itself: CLAUDE_problems.md P056, a third occurrence of the banned `python3` `open().write()` file-edit pattern (P038/P054), caught mid-turn and corrected -- see that entry for full RCA.** |
| Output Report pages converted to real Jinja2 templates via a `.tmpl.html` sibling-file convention, verified against 4 real scenarios before any live wiring (2026-08-30 02:45 EDT) | Scope of Open Item #2's first half, confirmed with Ben ("Proceed as recommended"): prove the finished 11-page design is genuinely data-driven -- not just internally consistent for one fixed scenario -- without touching `results.html`/`routes.py`/`mailer/generate.py` yet, so this work can't collide with Tristen's possible in-progress work on Open Item #17. Each locked page (`NN-name.html`) got a Jinja2-templated sibling in the same `output_report/` directory (`NN-name.tmpl.html`) rather than a parallel folder or subdirectory -- no relative `assets/...` path needed adjusting, and the original static pages stay untouched as the always-viewable locked reference deck. Template context is exactly `run_calculation()`'s real return shape (`results`) plus `session['goals']` (confirmed *not* part of `run_calculation()`'s return, so passed separately) plus `generated_date`, plus one Jinja2 global, `level_desc(key, level)`. Rather than hand-transcribe `assets/roadmap_data.js`'s `CURRENT_DESCRIPTIONS` object into Python for that global (real risk of silent drift between two copies of the same text), the render harness extracts it programmatically -- regex + `json.loads()` -- directly from the live JS source at build time, so there remains exactly one copy of that text in the whole project. Verified two ways before calling this done: (1) programmatically, a Playwright script loading all 44 render combinations (11 pages x 4 real scenarios -- test1/test2/test3/Skywalker) via `file://` and asserting exact computed values (bar widths, dot-fill counts, active zone-card selection, delta text, `CAPABILITY_LEVELS` JSON, footer company name) against each scenario's real source JSON; first run surfaced 60 false-positive "problems," all traced to the check itself rather than the templates -- CSS `text-transform:uppercase` (pre-existing, untouched by this conversion) makes Playwright's `inner_text()` return post-transform text, so case-sensitive string comparisons need to be case-insensitive; and `CAPABILITY_LEVELS` on the roadmap pages is deliberately IIFE-scoped (exactly as in the original static pages), never a `window` global, so it has to be read from the rendered page's own script-tag source rather than `window.CAPABILITY_LEVELS`. After fixing the check itself (not the templates, which were already correct): 44/44 pass. (2) visually, screenshotting the full Skywalker deck plus a representative page sample for test1/test2/test3, deliberately including the two most extreme synthetic scenarios (test2: every capability and goal-priority at maximum; test3: capabilities/priorities alternating hard between the two extremes) to catch layout breakage a pure attribute check would miss -- none found. See SESSION_LOG.md 2026-08-30 02:45 EDT entry for full per-page conversion detail. Deliberately not done: wiring any of this into `results.html`, `routes.py`, or `mailer/generate.py` -- that remains the rest of Open Item #2. |
